# MoKnow 项目设计文档

## 1. 项目概述

MoKnow 是一个面向日记和知识记录的 Markdown 桌面记事本软件。它以本地仓库为核心，将用户的 Markdown 文件按照“仓库 -> 目录 -> 文件”的层级进行存放、展示、编辑和检索。

项目目标不是做一个普通文本编辑器，而是做一个适合长期记录、回顾、整理和 AI 辅助分析的本地优先型 Markdown 工作台。

核心能力包括：

- 使用 Markdown 记录日记、笔记和知识内容。
- 支持源码编辑、双视图编辑、所见即所得编辑。
- 使用仓库、目录、文件的层级组织内容。
- AI Cockpit 可以访问仓库中的所有文件，支持总结、检索、问答和回顾。
- 支持插件功能，允许扩展编辑器、Markdown 渲染、命令、面板和 AI 工具。

## 2. 技术选型

### 2.1 桌面应用框架

推荐使用 Tauri 作为桌面应用外壳。

原因：

- 应用体积小，运行资源占用低。
- 适合本地文件系统访问、系统托盘、快捷键、窗口管理等桌面能力。
- 前端可以完整使用 React + TypeScript 技术栈。
- 原生能力由 Tauri 命令桥接，业务逻辑仍尽量放在 TypeScript 服务层中。

说明：

- Tauri 的系统能力层使用 Rust，但本项目的主要业务代码仍设计为 TypeScript。
- 如果后续要求“前后端完全 TypeScript”，可以切换为 Electron；整体业务架构、前端组件和数据模型不需要大改。

### 2.2 前端技术栈

- React：构建桌面端主界面。
- TypeScript：提供完整类型约束。
- Ant Design：作为 UI 组件库。
- markdown-it：渲染 Markdown 预览内容。
- CodeMirror 或 Monaco Editor：作为源码编辑器。
- Zustand 或 Redux Toolkit：管理前端应用状态。
- React Router：管理主界面路由和功能页。

### 2.3 应用后端与本地能力

应用后端采用“TypeScript 服务层 + Tauri 系统桥接”的方式：

- TypeScript 服务层负责仓库管理、文件树建模、编辑状态、索引任务、插件生命周期、AI 请求编排。
- Tauri 命令负责读取文件、写入文件、选择目录、监听文件变化、访问系统剪贴板等系统能力。
- 前端组件不直接操作本地文件，而是通过服务层调用统一接口。

## 3. 总体架构

项目分为五个核心层：

1. 桌面壳层
   负责窗口、菜单、系统权限、本地文件访问和系统级事件。

2. 前端表现层
   负责 React 页面、Ant Design 组件、编辑器、文件树、AI Cockpit 和插件面板展示。

3. 应用服务层
   负责业务逻辑，包括仓库管理、文件操作、Markdown 渲染、编辑模式切换、索引和插件管理。

4. 数据与索引层
   负责维护仓库目录树、文件元信息、全文索引、AI 上下文索引和用户配置。

5. 插件扩展层
   负责插件加载、权限控制、生命周期管理和扩展点注册。

推荐目录结构：

```text
MoKnow/
  src/
    app/
      App.tsx
      routes.tsx
      providers/
    components/
      layout/
      repository/
      editor/
      markdown/
      ai-cockpit/
      plugin/
    services/
      repositoryService.ts
      fileService.ts
      markdownService.ts
      editorService.ts
      aiService.ts
      pluginService.ts
      indexService.ts
    stores/
      repositoryStore.ts
      editorStore.ts
      aiStore.ts
      pluginStore.ts
    types/
      repository.ts
      file.ts
      editor.ts
      plugin.ts
      ai.ts
  src-tauri/
    src/
      commands/
      main.rs
  plugins/
  docs/
  1.项目创建.md
```

## 4. 核心功能设计

### 4.1 仓库管理

仓库是 MoKnow 的最高层级。一个仓库对应本机文件系统中的一个目录。

仓库能力：

- 创建仓库：选择或新建一个本地目录。
- 打开仓库：读取目录结构并生成文件树。
- 最近仓库：记录最近打开的仓库列表。
- 仓库配置：每个仓库可以保存独立配置，例如默认编辑模式、插件启用状态、AI 索引配置。

仓库目录中推荐包含一个隐藏配置目录：

```text
my-diary-repo/
  .moknow/
    config.json
    index/
    plugins.json
  2026/
    06/
      04.md
  ideas/
    product.md
```

### 4.2 目录与文件管理

文件树以“目录 -> Markdown 文件”为主体，支持常见操作：

- 新建目录。
- 新建 Markdown 文件。
- 重命名目录或文件。
- 移动目录或文件。
- 删除目录或文件。
- 在文件树中搜索文件名。
- 展开、折叠目录。

文件类型以 `.md` 为主，后续可以支持 `.markdown`、`.txt` 和附件资源。

为了适合日记场景，系统可以提供快捷创建日记文件的能力：

- 按日期创建，例如 `2026/06/04.md`。
- 如果当天文件已存在，则直接打开。
- 支持日记模板，例如标题、日期、天气、心情、待办等。

### 4.3 Markdown 编辑器

编辑器支持三种模式。

#### 源码模式

源码模式直接编辑 Markdown 原文。

适用场景：

- 用户熟悉 Markdown 语法。
- 需要精确控制标题、链接、代码块、表格等内容。

推荐使用 CodeMirror 或 Monaco Editor。

#### 双视图模式

双视图模式左侧为 Markdown 源码，右侧为渲染预览。

能力：

- 左侧输入，右侧实时预览。
- 滚动同步。
- 支持预览区域点击标题定位到源码。
- 支持文件保存状态提示。

#### 所见即所得模式

所见即所得模式提供接近最终渲染效果的编辑体验。

第一阶段可以采用“类所见即所得”方案：

- 用户在可视化编辑器中输入内容。
- 标题、列表、引用、代码块等以格式化样式显示。
- 底层仍保存为 Markdown。

实现方式建议：

- 第一阶段使用成熟编辑器库，如 Milkdown、TipTap + Markdown 序列化能力，或 Toast UI Editor。
- Markdown 渲染预览仍统一使用 markdown-it，避免预览规则分裂。

### 4.4 Markdown 渲染

Markdown 渲染使用 markdown-it。

基础能力：

- 标题、段落、列表、引用。
- 代码块和行内代码。
- 表格。
- 链接和图片。
- 任务列表。

可扩展能力：

- 代码高亮。
- Mermaid 图表。
- 数学公式。
- 目录生成。
- 自定义块语法。
- 插件自定义渲染规则。

渲染安全：

- 默认禁用不可信 HTML。
- 对用户开启 HTML 渲染的场景提供安全提示。
- 外部链接点击前通过桌面壳调用系统浏览器打开。

### 4.5 AI Cockpit

AI Cockpit 是 MoKnow 的智能工作区，可以访问当前仓库中的所有文件。

核心能力：

- 仓库问答：用户可以询问“我最近一周主要记录了什么？”。
- 文件总结：总结当前文件或选中目录。
- 日记回顾：按时间范围生成回顾。
- 语义检索：根据自然语言查找相关笔记。
- 写作辅助：改写、续写、生成标题、提取待办。
- 结构整理：把零散日记整理为主题、项目或行动项。

AI Cockpit 不直接读取 UI 状态，而是通过索引服务访问仓库内容。

推荐流程：

1. 用户打开仓库。
2. 索引服务扫描 Markdown 文件。
3. 对文件生成元信息、全文索引和可选向量索引。
4. 用户在 AI Cockpit 输入问题。
5. AI 服务根据问题检索相关文件片段。
6. 将检索结果、当前文件上下文和用户问题组装为 AI 请求。
7. 返回回答，并展示引用来源。

AI 权限设计：

- 默认只允许 AI 访问当前打开的仓库。
- 用户可以关闭某些目录或文件的 AI 访问权限。
- AI 回答必须尽量附带来源文件。
- 发送到外部模型前，应明确提示用户涉及的内容范围。

### 4.6 插件系统

插件系统用于扩展 MoKnow 的功能。

支持的扩展点：

- 命令：向命令面板注册操作。
- 菜单：向顶部菜单或右键菜单添加入口。
- 侧边栏：添加自定义侧边栏面板。
- 编辑器：添加工具栏按钮、快捷键、编辑器命令。
- Markdown：注册 markdown-it 插件或渲染增强能力。
- AI：注册 AI 工具，例如“生成周报”“提取情绪变化”。
- 文件：监听文件创建、保存、删除事件。

插件目录结构示例：

```text
plugins/
  daily-review/
    plugin.json
    dist/
      index.js
```

插件清单示例：

```json
{
  "id": "daily-review",
  "name": "Daily Review",
  "version": "0.1.0",
  "main": "dist/index.js",
  "permissions": ["repository.read", "ai.use", "markdown.render"],
  "activationEvents": ["onRepositoryOpen"]
}
```

插件安全策略：

- 插件必须声明权限。
- 插件默认不能访问仓库全部文件，除非声明并获得用户确认。
- 插件运行环境应与核心应用隔离。
- 插件异常不能导致主应用崩溃。

## 5. 页面与交互设计

### 5.1 主界面布局

主界面采用典型桌面工作台布局：

```text
+------------------------------------------------------+
| 顶部工具栏：仓库、保存、编辑模式、搜索、AI、插件      |
+-------------+--------------------------+-------------+
| 仓库文件树  | Markdown 编辑区          | AI Cockpit  |
|             | 源码 / 双视图 / WYSIWYG  | 或插件面板  |
+-------------+--------------------------+-------------+
| 状态栏：当前文件、保存状态、字数、索引状态            |
+------------------------------------------------------+
```

### 5.2 主要区域

左侧：仓库资源管理器

- 展示仓库、目录、文件。
- 支持新建、重命名、删除、移动。
- 支持快速筛选文件。

中间：编辑器

- 是最核心的工作区域。
- 顶部提供编辑模式切换。
- 支持保存状态、撤销重做、字数统计。

右侧：AI Cockpit / 插件面板

- 默认展示 AI Cockpit。
- 插件可以注册自己的面板。
- 用户可以折叠右侧区域。

底部：状态栏

- 显示当前仓库。
- 显示当前文件路径。
- 显示保存状态。
- 显示 AI 索引状态。

## 6. 数据模型

### 6.1 Repository

```ts
interface Repository {
  id: string;
  name: string;
  rootPath: string;
  openedAt: string;
  config: RepositoryConfig;
}
```

### 6.2 FileNode

```ts
interface FileNode {
  id: string;
  name: string;
  path: string;
  type: "directory" | "markdown" | "asset";
  children?: FileNode[];
  updatedAt?: string;
}
```

### 6.3 EditorState

```ts
interface EditorState {
  filePath: string;
  content: string;
  mode: "source" | "split" | "wysiwyg";
  isDirty: boolean;
  lastSavedAt?: string;
}
```

### 6.4 PluginManifest

```ts
interface PluginManifest {
  id: string;
  name: string;
  version: string;
  main: string;
  permissions: string[];
  activationEvents: string[];
}
```

## 7. 数据流设计

### 7.1 打开仓库

```text
用户选择目录
  -> Tauri 文件系统命令确认路径
  -> repositoryService 创建 Repository
  -> fileService 扫描目录树
  -> repositoryStore 更新文件树
  -> indexService 启动索引任务
  -> UI 展示仓库内容
```

### 7.2 编辑并保存文件

```text
用户编辑 Markdown
  -> editorStore 更新内容和 dirty 状态
  -> markdownService 渲染预览
  -> 用户触发保存或自动保存
  -> fileService 写入本地文件
  -> indexService 更新当前文件索引
  -> 状态栏显示保存成功
```

### 7.3 AI 问答

```text
用户输入问题
  -> aiService 分析问题
  -> indexService 检索相关文件片段
  -> aiService 组装上下文
  -> 调用 AI 模型
  -> 返回回答与引用来源
  -> AI Cockpit 展示结果
```

### 7.4 插件加载

```text
应用启动或打开仓库
  -> pluginService 读取插件清单
  -> 校验插件权限和版本
  -> 根据 activationEvents 激活插件
  -> 插件注册命令、面板或渲染规则
  -> UI 和服务层使用注册结果
```

## 8. 错误处理设计

### 8.1 文件系统错误

常见错误：

- 文件不存在。
- 文件被外部程序修改。
- 没有读写权限。
- 文件名非法。
- 删除或移动失败。

处理策略：

- 在 UI 中展示明确错误信息。
- 保存前检查文件是否被外部修改。
- 对危险操作提供确认弹窗。
- 保存失败时保留编辑器内容，避免用户输入丢失。

### 8.2 Markdown 渲染错误

处理策略：

- markdown-it 渲染失败时显示降级预览。
- 单个插件渲染失败不能阻断完整预览。
- 对自定义 Markdown 插件增加错误边界。

### 8.3 AI 错误

常见错误：

- AI 服务不可用。
- 请求超时。
- 仓库索引未完成。
- 上下文过长。
- 用户未配置 API Key。

处理策略：

- 展示 AI 状态和错误原因。
- 支持取消生成。
- 上下文过长时自动压缩或让用户选择范围。
- 索引未完成时允许基于当前文件回答。

### 8.4 插件错误

处理策略：

- 插件加载失败时禁用该插件。
- 插件运行异常时记录日志并提示用户。
- 插件不能直接修改核心应用状态，必须通过公开 API。
- 插件权限不足时返回明确错误。

## 9. 测试策略

### 9.1 单元测试

重点测试：

- repositoryService。
- fileService。
- markdownService。
- indexService。
- pluginService。
- aiService 的请求组装逻辑。

### 9.2 组件测试

重点测试：

- 文件树展示与操作。
- 编辑模式切换。
- Markdown 预览渲染。
- AI Cockpit 消息展示。
- 插件面板挂载。

### 9.3 集成测试

重点测试：

- 打开仓库。
- 创建日记文件。
- 编辑、保存、重新打开文件。
- 修改文件后更新索引。
- AI Cockpit 能读取仓库内容并返回引用来源。
- 插件注册命令后能被调用。

### 9.4 桌面端测试

重点测试：

- 本地目录选择。
- 文件读写权限。
- 跨平台路径处理。
- 应用启动和关闭。
- 自动保存和异常退出恢复。

## 10. 开发阶段规划

### 第一阶段：项目骨架

目标：

- 创建 Tauri + React + TypeScript 项目。
- 接入 Ant Design。
- 建立基础布局。
- 实现仓库打开和文件树展示。

交付结果：

- 用户可以打开一个本地目录。
- 左侧可以看到目录和 Markdown 文件。

### 第二阶段：Markdown 编辑

目标：

- 实现源码编辑。
- 使用 markdown-it 渲染预览。
- 实现双视图模式。
- 实现文件保存。

交付结果：

- 用户可以编辑和保存 Markdown 文件。
- 用户可以在源码和双视图之间切换。

### 第三阶段：日记工作流

目标：

- 实现按日期快速创建日记。
- 支持日记模板。
- 支持最近文件和最近仓库。

交付结果：

- 用户可以快速创建当天日记。
- 日记文件按日期目录归档。

### 第四阶段：AI Cockpit

目标：

- 建立仓库索引。
- 实现仓库问答。
- 支持当前文件总结。
- 支持引用来源展示。

交付结果：

- AI Cockpit 可以访问仓库内 Markdown 文件。
- 用户可以基于仓库内容提问。

### 第五阶段：插件系统

目标：

- 定义插件 manifest。
- 实现插件加载和权限声明。
- 支持命令扩展、侧边栏面板扩展、Markdown 渲染扩展。

交付结果：

- 用户可以安装并启用基础插件。
- 插件可以扩展编辑器和 Markdown 渲染。

### 第六阶段：所见即所得编辑

目标：

- 引入成熟 WYSIWYG Markdown 编辑方案。
- 与现有 Markdown 内容保存格式打通。
- 保持源码、双视图、所见即所得三种模式一致。

交付结果：

- 用户可以在可视化模式下编辑 Markdown。
- 三种编辑模式可以稳定切换。

## 11. 关键设计原则

- 本地优先：用户数据默认存放在本机仓库目录中。
- Markdown 优先：文件保存格式以 Markdown 为主，避免被专有格式锁定。
- 可扩展：插件系统应服务于长期扩展，而不是只做一次性功能。
- AI 可追溯：AI 回答应尽量展示来源文件，避免黑盒结论。
- 编辑安全：保存失败、渲染失败、插件失败都不能导致用户内容丢失。
- 渐进实现：先完成稳定的源码和双视图编辑，再实现所见即所得编辑。

## 12. 初始结论

MoKnow 应先构建一个稳定的本地 Markdown 桌面工作台：以仓库管理和 Markdown 编辑为基础，以 AI Cockpit 作为核心增强能力，以插件系统作为长期扩展能力。

第一版不应过早追求复杂插件市场或完整所见即所得体验，而应优先保证：

- 本地仓库打开稳定。
- 文件树清晰。
- Markdown 编辑和预览可靠。
- 文件保存安全。
- AI Cockpit 能准确读取仓库内容并提供来源引用。

在这个基础上，再逐步扩展日记模板、插件系统和更成熟的所见即所得编辑体验。
