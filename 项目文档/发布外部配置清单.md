# 发布外部配置清单

| 项目 | 值 |
|---|---|
| 生成时间 | 2026-06-12T04:42:51.488Z |
| 严格模式 | 否 |
| 缺失项数量 | 8 |

| 外部项 | 状态 | 环境变量状态 | 说明 |
|---|---|---|---|
| AI 真实外部 Provider 联调 | 缺失 | `AI_E2E_ENDPOINT`: 缺失<br>`AI_E2E_MODEL`: 缺失<br>`AI_E2E_API_KEY`: 缺失 | 用于执行 npm run ai:e2e 并生成真实外部模型联调记录。 |
| Tauri updater 签名 | 缺失 | `TAURI_SIGNING_PRIVATE_KEY`: 缺失 | 用于生成 updater bundle 的 .sig 文件；如私钥有密码，还需 TAURI_SIGNING_PRIVATE_KEY_PASSWORD。 |
| Tauri updater 公钥与端点 | 缺失 | `MOKNOW_UPDATER_PUBKEY`: 缺失<br>`MOKNOW_RELEASE_DOWNLOAD_BASE_URL`: 缺失 | release:updater-config 会用这些值生成 pubkey 和 latest.json endpoint。 |
| macOS Developer ID 签名 | 缺失 | `APPLE_CERTIFICATE`: 缺失<br>`APPLE_CERTIFICATE_PASSWORD`: 缺失 | 用于 macOS app / dmg 签名；APPLE_SIGNING_IDENTITY 可选。 |
| macOS 公证 | 缺失 | 方案 1: `APPLE_ID`: 缺失<br>`APPLE_PASSWORD`: 缺失<br>`APPLE_TEAM_ID`: 缺失<br>方案 2: `APPLE_API_KEY`: 缺失<br>`APPLE_API_ISSUER`: 缺失 | Apple ID 模式或 App Store Connect API 模式满足一组即可。 |
| Windows code signing | 缺失 | 方案 1: `WINDOWS_CERTIFICATE`: 缺失<br>`WINDOWS_CERTIFICATE_PASSWORD`: 缺失<br>方案 2: `TAURI_WINDOWS_SIGNTOOL_PATH`: 缺失 | 证书模式或自定义 signtool / 云签名命令满足一组即可。 |
| 下载与官网分发 URL | 缺失 | `MOKNOW_RELEASE_DOWNLOAD_BASE_URL`: 缺失<br>`MOKNOW_HOMEPAGE_URL`: 缺失 | 用于 downloads.json、Homebrew cask、官网/项目主页链接。 |
| Homebrew tap | 缺失 | `MOKNOW_HOMEBREW_TAP`: 缺失 | 用于记录正式 cask 发布目标，例如 owner/homebrew-tap。 |

## 使用方式

```bash
npm run release:readiness
npm run release:readiness -- --strict --write-doc
```

> 报告只显示变量是否配置和少量非敏感标识，不写入 API Key、证书、私钥或密码。
