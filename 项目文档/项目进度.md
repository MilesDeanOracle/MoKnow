# 项目进度核对记录

更新时间：2026-06-12

本次核对范围：

- `README.md`
- `项目文档/08-功能增加与优化清单.md`
- `项目文档/09-软件功能第一期-日记主线.md`
- `项目文档/10-软件功能第二期-日历与时间组织.md`
- `项目文档/11-软件功能第三期-编辑与记录体验.md`
- `项目文档/12-软件功能第四期-检索标签与整理.md`
- `项目文档/13-软件功能第五期-安全备份与长期使用.md`
- `项目文档/个人AI日记产品规划.md`
- `项目文档/初创/01-第一期-基础框架搭建.md`
- `项目文档/初创/02-第二期-编辑器三模式.md`
- `项目文档/初创/03-第三期-数据管理与搜索.md`
- `项目文档/初创/04-第四期-AI-Copilot.md`
- `项目文档/初创/05-第五期-插件系统与主题.md`
- `项目文档/初创/06-第六期-打磨与发布.md`
- `项目文档/初创/MarkNote-技术设计方案.md`
- `src/`
- `src-tauri/`

## 一、验证结果

已通过：

```bash
npm run test
npm run build
RUSTC=$(rustup which --toolchain stable rustc) RUSTDOC=$(rustup which --toolchain stable rustdoc) rustup run stable cargo test
RUSTC=$(rustup which --toolchain stable rustc) RUSTDOC=$(rustup which --toolchain stable rustdoc) npm run tauri:build
```

验证结论：

- 前端测试：16 个测试文件、148 个测试用例通过。
- 前端构建：通过。
- Rust 测试：23 个单元测试通过，doc test 通过。
- AI 真实 Provider 自动探测：`npm run ai:e2e:auto -- --write-doc` 可自动探测环境变量、Ollama 和 LM Studio；当前环境未发现可用真实 Provider，因此未执行真实 E2E。
- AI 真实联调预检：`npm run ai:e2e:preflight -- --write-doc` 通过并写入 `项目文档/AI外部模型联调记录.md`；当前 endpoint、model 和 API Key 缺失，`npm run ai:e2e:preflight -- --strict` 会按预期失败，未调用真实模型且未记录敏感凭据。
- AI 联调脚本自测：`npm run ai:e2e:mock -- --write-doc` 通过，已启动本地 OpenAI-compatible mock Provider，验证鉴权、非流式 JSON 解析和 SSE 流式解析，并写回 `项目文档/AI外部模型联调记录.md`；该自测不等同于真实外部模型联调。
- 发布静态检查：`npm run release:check` 通过；外部 AI、updater key、签名、公证和分发 URL 缺失会作为 warning，`--strict-external` 可升级为失败。
- 外部发布配置 readiness：`npm run release:readiness -- --write-doc` 通过，已生成脱敏外部配置报告并写入 `项目文档/发布外部配置清单.md`；模拟配置全部外部项后 `npm run release:readiness -- --strict` 可通过。
- Updater 临时配置生成：缺少 `MOKNOW_UPDATER_PUBKEY` 时 `npm run release:updater-config` 会失败；提供公钥和 HTTPS Release 下载 URL 时可生成包含 `createUpdaterArtifacts`、`pubkey` 和 `endpoints` 的临时 Tauri 配置；使用临时有效公钥执行 `tauri build --config /tmp/moknow-updater-valid.json --no-bundle --ci` 通过，证明配置可被 Tauri CLI 合并并完成 release 编译。
- 严格外部检查模拟：提供 AI、updater、公证、签名和分发 URL 环境变量后，`npm run release:check -- --strict-external` 可通过；Release workflow YAML 解析通过。
- 本机发布产物检查：`npm run release:artifacts` 通过，已识别 macOS DMG 产物；当前无 updater `.sig`，正式发布构建需配置 `TAURI_SIGNING_PRIVATE_KEY`。
- 本机分发元数据生成：`npm run release:metadata` 通过，已生成 `release/downloads.json`、`release/latest.json` 和 `release/homebrew/moknow.rb`；当前因无 updater `.sig`，`latest.json` 暂无可用更新平台条目。
- 下载 artifacts 完整性验证：`npm run release:downloads -- --artifact-root release/downloaded-artifacts --platform macos --write-doc` 通过，已将本机 macOS bundle 模拟下载目录的完整性检查写入 `项目文档/发布产物验证记录.md`；默认三平台检查会因缺 Windows/Linux 下载产物失败，符合当前未远端实跑状态。
- 安装冒烟验证：`npm run release:install-smoke -- --write-doc` 通过，已识别本机 macOS `.app`、bundle identifier、版本、可执行文件和 DMG 镜像，并将人工打开、仓库创建/打开、Markdown 保存和重启恢复清单写入 `项目文档/发布产物验证记录.md`；实机打开仍需人工执行。
- 人工安装验收记录：新增 `npm run release:install-manual`，实机安装、打开、仓库读写、Markdown 保存和重启恢复验证完成后，可用 `npm run release:install-manual -- --result passed --confirm-all --write-doc` 生成持久化人工记录；`release:install-smoke` 会读取该记录，避免 `release:final-audit -- --refresh` 把人工通过状态覆盖回“待人工”。
- 签名与公证验证：`npm run release:signing -- --write-doc` 通过并生成 `项目文档/发布签名验证记录.md`；当前 macOS app 仅为 ad-hoc/linker-signed 且 Gatekeeper 验证未通过，DMG 未签名且未装订公证票据；`npm run release:signing -- --require-signed --require-notarized` 会按预期失败，正式发布需外部证书和公证配置后重跑。
- 远端 CI 预检：`npm run release:ci-preflight -- --write-doc` 通过并生成 `项目文档/发布CI预检记录.md`；GitHub CLI 已登录且 token 具备 `workflow` 权限，本地 Release / Release Dry Run workflow 内容检查通过；但 workflow 文件当前未被 git 跟踪，远端 `release-dry-run.yml` 返回 404，`npm run release:ci-preflight -- --strict` 会按预期失败，需提交推送后才能触发远端 dry-run。
- Release workflow 结构验证：新增 `npm run release:workflow-validate`，可解析 Release / Release Dry Run workflow YAML 并检查三平台矩阵、build / verify job、artifact 上传下载、AI 证据、聚合验证和正式 Release 严格签名分发门禁；当前本地 workflow 结构验证通过，Release / Release Dry Run 的 Ubuntu verify job 已接入该命令并上传 `workflow-validation.json` / `.md`。
- 远端发布状态快照：新增 `npm run release:remote-status`，可读取远端 Release / Release Dry Run workflow、最近 runs、artifacts 和 GitHub Release 状态，生成 `项目文档/发布远端状态记录.md`；当前远端 workflow 仍未发现，严格模式会失败。
- 三平台 artifacts 证据聚合：`npm run release:aggregate-evidence -- --artifact-root release --output-dir release/aggregate-test` 通过，已验证可从现有 release 报告聚合安装冒烟和签名验证 JSON；Release / Release Dry Run workflow YAML 解析通过，均包含矩阵后的 `verify` job。
- 分发端点验证：`npm run release:distribution -- --write-doc` 通过并生成 `项目文档/发布分发验证记录.md`；当前下载 URL 为空、`latest.json` 无 updater 平台条目、Homebrew cask 仍含 `OWNER/REPO` 占位 URL、未配置 updater endpoint / Homebrew tap，严格模式会失败。
- GitHub Actions secrets 审计：`npm run release:secrets -- --write-doc` 通过并生成 `项目文档/发布Secrets审计记录.md`；当前仓库未发现发布所需 secrets，严格模式会失败。
- GitHub Actions secrets 同步预览：`npm run release:secrets:sync -- --write-doc` 可读取当前 shell 的外部变量，生成不会泄露 secret 值的同步预览和 `项目文档/发布Secrets同步记录.md`；显式加 `--apply --verify` 后才会写入远端并重新执行 secrets 审计。
- 外部闭环交接清单：`npm run release:handoff -- --write-doc` 通过并生成 `项目文档/发布外部闭环交接清单.md` 和 `项目文档/发布外部闭环环境变量模板.env`，已把 AI 真实联调、远端三平台 CI、GitHub secrets、签名、公证、updater、Homebrew / 官网分发的配置项、发布提交入库 pathspec 计划和最终验收命令集中到一个交接包；该清单不包含真实密钥。
- 外部闭环总门禁：新增 `npm run release:closure`，可串行运行入库、AI 真实联调、远端 CI、三平台下载/安装、secrets、readiness、签名、公证、metadata、分发和最终审计严格门禁，生成 `项目文档/发布外部闭环总门禁记录.md`；当前会集中记录缺真实 Provider、远端 workflow / 三平台 artifacts、证书和分发 URL 的阻断点。
- 发布文件入库门禁：`npm run release:tracking -- --write-doc` 可生成 `项目文档/发布文件入库检查记录.md`，检查 Release workflow、发布脚本、Makefile 和项目文档是否存在、是否被 `.gitignore` 忽略、是否已纳入 git；`--strict` 可在提交前阻断漏入库文件。
- 发布提交入库计划：`npm run release:submit-prep -- --write-doc` 可生成 `项目文档/发布提交准备总览.md`，一键刷新提交安全、提交计划、提交预演、推送准备和 CI 预检报告；`npm run release:submission-safety -- --write-doc` 可生成 `项目文档/发布提交安全检查记录.md`，扫描发布闭环 pathspec / 必需文件中的密钥、证书、私钥、生成产物和额外路径风险；`npm run release:submission-plan -- --write-doc` 可生成 `release/submission-plan.*`、NUL 分隔的 `release/submission-pathspec.txt` 和 `项目文档/发布提交入库计划.md`，推荐用 `git add --pathspec-from-file=release/submission-pathspec.txt --pathspec-file-nul` 安全纳入中文路径、workflow、脚本、项目文档、`package.json` / lockfile 和 Tauri updater / Keychain 支撑文件；脚本会用临时 index / 临时 object 目录执行 `git add --dry-run` 和严格入库模拟，不会自动 stage / commit / push；`npm run release:submission-preview -- --write-doc` 会生成 `项目文档/发布提交预演记录.md`，用临时 index 预演提交 tree、diff 统计和变更文件；`npm run release:push-readiness -- --write-doc` 会生成 `项目文档/发布推送准备记录.md`，记录当前分支、upstream、GitHub CLI 登录和远端 workflow 状态。
- Dry Run 命令编排：`npm run release:dry-run -- --print-only --ref main --write-doc` 通过，输出中包含 `release:workflow-validate`、`release:ci-preflight`、远端 dry-run 触发、`release:remote-status -- --mode dry-run`、`release:downloads`、`release:install-smoke`、`release:signing`、`release:aggregate-evidence` 和 `release:final-audit`。
- 最终进度审计：`npm run release:final-audit -- --write-doc` 可把 AI 真实联调、远端三平台 CI、下载/安装、签名公证和分发元数据证据汇总写回本文档；当前仍会把 3 个外部闭环项判定为未完成。
- Tauri macOS aarch64 构建：通过。
- Tauri 产物：
  - `src-tauri/target/release/bundle/macos/MoKnow.app`
  - `src-tauri/target/release/bundle/dmg/MoKnow_0.1.0_aarch64.dmg`

验证备注：

- `npm run build` 已不再提示单个 JS chunk 超过 500 kB；应用主入口约 218.21 kB，当前最大应用 JS chunk 约 300.08 kB；`EditorWorkspace` chunk 已降到约 204.24 kB，CodeMirror 相关依赖已拆为约 208.22 kB / 197.32 kB 等按需 chunk；KaTeX 公式运行时已拆为约 259.24 kB 的按需 chunk；Mermaid 图表运行时已移出 Vite 应用 chunk 图谱，构建产物为 `dist/vendor/mermaid` 下 82 个 `.mjs` 文件、约 3.7M，仅在文档包含 Mermaid 图表时按需加载。
- 当前 shell 默认优先使用 Homebrew rustc 1.87.0，会导致 Tauri/Rust 依赖版本不兼容；已在 Makefile 的非 Windows Tauri 目标中优先使用 rustup stable。

## 二、已完成或基本完成

### 1. 基础工程

状态：已完成。

证据：

- `package.json` 已配置 Vite、React、TypeScript、Vitest、Tauri CLI。
- `src-tauri/Cargo.toml` 已配置 Tauri v2、rusqlite、dialog plugin。
- 前端测试、前端构建、Rust 测试和 macOS Tauri 构建均通过。

### 2. 工作台 UI

状态：基本完成。

证据：

- `src/App.tsx` 组合标题栏、仓库树、编辑器、AI Cockpit、状态栏。
- `src/styles/app.css` 提供暗色工作台样式。
- `ThemeService` 支持 day / night / system / custom 主题偏好解析。

缺口：

- 仍偏“Markdown 编辑器壳”，日历主线和专注写作已有入口，但还没有日记首页。

### 3. 仓库与文件读写底座

状态：后端底座已完成，前端部分入口已完成。

证据：

- `RepositoryService::create_repository` 创建 `.moknow`、`日记`、`文档`、`回收站`。
- `RepositoryService::open_repository`、`open_last_repository`、`list_recent_repositories` 支持真实仓库打开和最近仓库。
- `RepositoryService::load_repository` 扫描真实 Markdown 文件树。
- `RepositoryService::read_markdown_file` 和 `save_markdown_file` 读写真实文件。
- `src/App.tsx` 已接入新建仓库、切换仓库、新建日记、新建文档、手动保存和自动保存。

缺口：

- 浏览器预览仍使用 mock gateway，这是预览模式设计，不代表桌面真实命令未实现。
- 权限错误、磁盘空间不足等场景仍缺少更细化的处理建议。

### 4. SQLite 元数据底座

状态：部分完成。

证据：

- `open_database` 创建 `repositories`、`file_metadata`、`favorites`、`trash_records` 表。
- Rust 测试验证 SQLite 可作为普通数据库读取。

缺口：

- `file_metadata` 已用于最近打开、最近编辑、置顶和 frontmatter 元数据同步；`tags` / `file_tags` 已用于全局标签颜色和跨文档标签维护；`file_fts` 已用于全文搜索索引。

### 5. 回收站、收藏、重命名和系统文件夹显示

状态：基础闭环完成。

证据：

- `move_to_trash`、`restore_from_trash`、`permanently_delete_trash_entry`、`clear_trash` 已实现。
- `add_favorite`、`remove_favorite` 已实现。
- `rename_entry` 已实现并同步收藏路径。
- `show_in_folder` 已调用系统文件管理器打开对应位置。
- `CommandGateway` 和 `RepositoryService` 已暴露对应方法。
- `RepositoryTree` 已提供移入回收站、恢复、彻底删除、清空回收站、重命名、显示位置、收藏和置顶按钮。

缺口：

- 回收站恢复目前默认使用自动改名策略，没有恢复冲突策略选择 UI。

### 6. 编辑器三模式

状态：已完成基础验收。

证据：

- `EditorWorkspace` 支持 source、split、wysiwyg 三种模式。
- `MarkdownService` 使用 `markdown-it` 渲染基础 Markdown。
- `EditorWorkspace` 已接入 CodeMirror 6，源码模式和双视图源码栏使用真实 CodeMirror 编辑器，保留同步 fallback textarea 作为无障碍和测试路径。
- 所见即所得模式已接入 TipTap，并通过 `htmlMarkdownConversionService` 将富文本 HTML 回写为 Markdown。
- Markdown 快捷插入、任务列表点击勾选、当前文件查找替换、大纲导航、双视图同步滚动、图片粘贴/拖拽和双链跳转仍保持可用。

### 7. AI Cockpit

状态：真实 Provider 配置底座、本地日记助手和本地轻量 RAG 检索已完成。

证据：

- `AiCockpitService` 支持本地规则模式和 OpenAI-compatible Provider 调用。
- `AiCockpit` 有聊天面板和快捷按钮。
- `AiCockpit` 可配置 AI endpoint、模型、流式响应开关和 API Key；API Key 只保存到系统凭据 / mock secure store。
- 普通提问可走真实 provider；未配置、缺少 Key 或请求失败时会回退本地规则回答。
- `AiCockpitService.runDiaryAction` 可根据当前文件、最近 7 天、本月日记或全仓库上下文生成今日总结、温柔追问、周回顾和月回顾。
- `AiCockpit` 支持上下文范围切换，并可把 AI 输出插入当前正文。
- `AiVectorIndexService` 可为当前范围 Markdown 构建本地轻量向量索引，并用词频向量、余弦相似度和中英文查询扩展做语义召回。
- `AiVectorIndexService` 支持 OpenAI-compatible / Ollama 外部 Embedding 设置、API Key 凭据读取、本地持久化向量缓存和索引构建进度回调。
- `AiCockpit` 提供 Embedding Provider、预设、endpoint、模型、API Key、持久化开关配置，并展示索引构建阶段、百分比和失败回退提示。
- `App.loadAiContext` 在最近 7 天、本月和全仓库模式下优先使用向量检索召回 AI 上下文，索引为空、未就绪或外部 Embedding 失败时会把提示带入 AI 回复并回退。

缺口：

- 真实外部模型端到端验证仍需补齐。

### 8. 主题

状态：基础完成。

证据：

- `ThemeService` 可构建 Ant Design theme 和 CSS variables，并能解析 day / night / system / custom / plugin 偏好。
- 标题栏主题按钮会持久化 day / night 选择。
- `AppSettingsModal` 提供通用设置面板，可设置主题偏好、自定义主色、圆角、字号、紧凑模式和自定义 CSS，并可导入 CSS 文件、导入/导出设置 JSON、跳转到日记设置与应用锁设置。
- `PluginService` 可解析 manifest 中带主题配置的 `contributes.themes`，按 `theme` 权限收集为运行时插件主题。
- `AppSettingsModal` 支持选择插件主题，也支持当前仓库和当前文件的主题 / CSS 覆盖。
- `resolveScopedUserSettings` 会按全局、仓库、文件顺序合成有效设置，`App` 用有效设置渲染主题和注入 CSS。

### 9. Makefile

状态：已完成。

证据：

- 根目录 `Makefile` 提供中文帮助。
- `make start` 可安装依赖并启动前端预览。
- `make desktop` / `make desktop-build` 可启动或构建 Tauri 桌面应用。

### 10. 保存可靠性

状态：基础闭环完成。

证据：

- `App.save` 在手动保存和自动保存前调用 `checkFileChanged` 检测外部修改。
- 编辑内容变为 dirty 后 2 秒自动保存，并在状态栏显示“有未保存更改 / 正在保存 / 已保存 / 保存失败 / 外部修改冲突”。
- 保存失败会通过 `message.error` 和顶部通知提示，不会清空当前草稿。
- dirty 草稿会写入 `localStorage`，再次打开同一文件时自动恢复，保存成功后清理草稿备份。
- `MarkdownFilePayload` 和 `SavePayload` 已返回 `modifiedAt`；Rust 测试 `markdown_modified_time_supports_external_change_checks` 覆盖真实文件修改时间检查。
- 外部修改冲突弹窗提供“保留当前版本 / 加载外部版本 / 另存副本”三种处理方式。
- `save_markdown_file_as_copy` 会在原文件同目录创建唯一副本，不覆盖原文件。
- 前端测试覆盖自动保存、草稿恢复、修改时间变化检测和冲突弹窗操作；Rust 测试覆盖真实文件修改时间检查和另存副本。

## 三、本轮完成

| 优先级 | 功能模块 | 已完成内容 | 代码证据 | 来源需求方案 |
|---|---|---|---|---|
| P0 | 日记主线 | 启动真实仓库后自动打开今天日记 | `App.openStartupFile` 会读取工作区状态，并在 `autoOpenToday` 开启时调用 `openTodayJournal` | `项目文档/08-功能增加与优化清单.md`、`项目文档/09-软件功能第一期-日记主线.md`、`项目文档/个人AI日记产品规划.md` |
| P0 | 日记主线 | 今天日记不存在时自动创建，存在时直接打开且不重复创建 | Tauri 命令 `open_today_journal` 调用 `RepositoryService::open_today_journal`；Rust 测试 `open_today_journal_creates_missing_file_without_duplicates` 覆盖 | `项目文档/08-功能增加与优化清单.md`、`项目文档/09-软件功能第一期-日记主线.md`、`项目文档/个人AI日记产品规划.md` |
| P0 | 日记主线 | 默认日记模板和日期/星期变量替换 | `DiarySettingsPayload::default` 提供默认模板；`render_diary_template` 支持 `{{date}}`、`{{weekday}}`、`{{year}}`、`{{month}}`、`{{day}}`、`{{time}}`；Rust 测试覆盖日期和星期替换 | `项目文档/08-功能增加与优化清单.md`、`项目文档/09-软件功能第一期-日记主线.md`、`项目文档/个人AI日记产品规划.md` |
| P0 | 日记主线 | 默认日记路径规则按年/月生成 | `today_diary_relative_path` 默认生成 `日记/{YYYY}/{MM}/{YYYY-MM-DD}.md`，并自动创建缺失目录 | `项目文档/08-功能增加与优化清单.md`、`项目文档/09-软件功能第一期-日记主线.md` |
| P0 | 日记主线 | 恢复上次打开文件和编辑模式的底座 | 新增 `get_workspace_state` / `save_workspace_state`；App 在选择文件、保存、切换编辑模式时写入状态；前端测试 `persists the last opened file and edit mode` 覆盖 | `项目文档/08-功能增加与优化清单.md`、`项目文档/09-软件功能第一期-日记主线.md`、`项目文档/个人AI日记产品规划.md` |
| P0 | 日记主线 | 日记设置面板：日记根目录、路径规则、文件名规则、模板编辑、启动自动打开开关 | 新增 `DiarySettingsModal`；标题栏设置按钮可打开并保存；Tauri 命令 `get_diary_settings` / `save_diary_settings` 已注册；前端测试覆盖设置保存和按自定义规则创建今天日记 | `项目文档/08-功能增加与优化清单.md`、`项目文档/09-软件功能第一期-日记主线.md`、`项目文档/个人AI日记产品规划.md` |
| P0 | 日记主线 | 恢复上次打开文件的光标位置 | `WorkspaceStateRecord` 新增 `last_cursor_position`；`save_workspace_state` 支持保存 line/column；`EditorWorkspace` 会报告并恢复 textarea selection；前端测试覆盖行列保存和恢复 | `项目文档/08-功能增加与优化清单.md`、`项目文档/09-软件功能第一期-日记主线.md`、`项目文档/个人AI日记产品规划.md` |
| P0 | 日历与时间组织 | 月历视图、月份切换、回到今天、已写日期标记、点击日期打开或创建日记 | 新增 `DiaryCalendar`；侧边栏展示月历；Tauri 命令 `list_diary_month_status` / `open_journal_by_date` 已注册；前端测试覆盖月历状态、翻月、按日期打开和自动创建 | `项目文档/08-功能增加与优化清单.md`、`项目文档/10-软件功能第二期-日历与时间组织.md`、`项目文档/个人AI日记产品规划.md` |
| P0 | 最近与收藏 | 最近文件、最近编辑、清空最近记录、置顶文件、收藏/取消收藏前端入口 | `file_metadata` 增加 `last_opened_at`、`last_edited_at`、`is_pinned`；新增 `list_recent_files` / `clear_recent_files` / `set_file_pinned`；`RepositoryTree` 展示最近打开/最近编辑，并提供置顶和收藏按钮；前端测试覆盖最近记录、清空和置顶排序 | `项目文档/08-功能增加与优化清单.md`、`项目文档/10-软件功能第二期-日历与时间组织.md` |
| P1 / P2 | 文件管理 UI | 回收站删除/恢复/彻底删除/清空、重命名、系统文件夹显示 | `RepositoryTree` 已提供文件管理操作入口；`show_in_folder` 改为真实系统命令；`App` 接入 `moveToTrash`、`restoreFromTrash`、`permanentlyDeleteTrashEntry`、`clearTrash`、`renameEntry`、`showInFolder`；前端测试覆盖回收站和文件管理按钮 | `项目文档/08-功能增加与优化清单.md`、`项目文档/13-软件功能第五期-安全备份与长期使用.md` |
| P1 | 保存可靠性 | 定时自动保存、保存失败明确提示、异常退出草稿恢复、保存前外部修改检测 | `App.save` 统一手动/自动保存；dirty 2 秒后自动保存；`localStorage` 保存并恢复未保存草稿；`check_file_changed` 比对 `modifiedAt` 并在冲突时暂停保存；状态栏展示保存状态；前端和 Rust 测试覆盖 | `项目文档/08-功能增加与优化清单.md`、`项目文档/初创/02-第二期-编辑器三模式.md`、`项目文档/13-软件功能第五期-安全备份与长期使用.md` |
| P1 / P2 | 冲突处理 | 保留当前版本、加载外部版本、另存副本 | `App` 在保存冲突时展示处理弹窗；保留当前版本会跳过冲突检查并覆盖保存；加载外部版本会重新读取磁盘文件并清理草稿；另存副本调用 `save_markdown_file_as_copy` 创建新文件并打开；前端和 Rust 测试覆盖 | `项目文档/13-软件功能第五期-安全备份与长期使用.md` |
| P1 | 编辑器实现 | CodeMirror 代码编辑器、TipTap 所见即所得编辑器、HTML 到 Markdown 转换 | `EditorWorkspace` 在源码模式和双视图源码栏接入 CodeMirror 6，并保留同步 fallback textarea；所见即所得模式接入 TipTap StarterKit、任务列表、图片、表格、高亮等扩展；新增 `htmlMarkdownConversionService` 使用 Turndown + GFM 将 TipTap HTML 回写 Markdown，并保留任务列表和双链语法；前端测试覆盖 CodeMirror 渲染、TipTap 渲染和 HTML->Markdown 转换 | `项目文档/初创/01-第一期-基础框架搭建.md`（CodeMirror 6 代码编辑器）、`项目文档/初创/02-第二期-编辑器三模式.md`（TipTap WYSIWYG、HTML->MD 转换决策）、`项目文档/初创/MarkNote-技术设计方案.md`（编辑器技术选型） |
| P0 / P1 | 编辑体验 | Markdown 快捷输入、快捷插入日期、快捷插入链接、任务列表点击勾选、查找替换 | `EditorWorkspace` 工具栏和快捷键支持加粗、斜体、标题、行内代码、日期、任务、链接；当前文件查找/替换面板支持查找、上下跳转、替换当前、全部替换；预览和所见即所得中的任务复选框点击后会同步修改 Markdown 源文；前端测试覆盖 | `项目文档/08-功能增加与优化清单.md`、`项目文档/11-软件功能第三期-编辑与记录体验.md`、`项目文档/初创/02-第二期-编辑器三模式.md` |
| P1 | 图片与附件 | 粘贴图片、拖拽图片、附件目录、Markdown 图片链接 | `save_attachment` 会把图片保存到当前 Markdown 同级 `assets/{文件名}/` 目录并返回相对 Markdown 图片链接；`EditorWorkspace` 支持粘贴/拖拽 `image/*` 文件并自动插入链接；前端和 Rust 测试覆盖 | `项目文档/08-功能增加与优化清单.md`、`项目文档/11-软件功能第三期-编辑与记录体验.md`、`项目文档/个人AI日记产品规划.md` |
| P1 | 编辑体验 | 大纲导航 | `EditorWorkspace` 会根据 Markdown 标题生成右侧文档大纲；大纲支持折叠/展开；点击标题会把编辑光标定位到对应标题行；前端测试覆盖标题提取、点击跳转和折叠恢复 | `项目文档/08-功能增加与优化清单.md`、`项目文档/11-软件功能第三期-编辑与记录体验.md`、`项目文档/个人AI日记产品规划.md` |
| P1 | 编辑体验 | 专注写作模式基础能力 | 标题栏新增“进入/退出专注写作”开关；进入后隐藏文件树和 AI Cockpit，主编辑器居中显示；退出后恢复完整工作台；前端测试覆盖进入/退出和两侧面板隐藏恢复 | `项目文档/08-功能增加与优化清单.md`、`项目文档/11-软件功能第三期-编辑与记录体验.md` |
| P1 | 编辑体验 | 专注写作高级能力：系统全屏、打字机模式 | 标题栏新增“开启/关闭打字机模式”和“进入/退出全屏写作”按钮；开启打字机模式会自动进入专注写作并让当前输入行保持在编辑区中部；全屏写作调用 Fullscreen API 并自动进入专注写作；前端测试覆盖 | `项目文档/08-功能增加与优化清单.md`、`项目文档/11-软件功能第三期-编辑与记录体验.md` |
| P1 | 编辑体验 | 双视图同步滚动 | `EditorWorkspace` 在双视图模式下会按滚动比例同步源码 textarea 和 Markdown 预览容器；前端测试覆盖源码滚动带动预览滚动 | `项目文档/初创/02-第二期-编辑器三模式.md` |
| P1 | 检索与标签 | frontmatter 解析与写回 | 新增 `frontmatterService`，提供 `parseFrontmatter`、`stringifyFrontmatter`、`updateFrontmatter`；支持保留正文、稳定写回、数组/布尔/数字/空值解析和更新时保留未知字段；前端单元测试覆盖 | `项目文档/12-软件功能第四期-检索标签与整理.md`、`项目文档/个人AI日记产品规划.md` |
| P1 | 检索与标签 | 情绪、天气、标签、地点、精力等日记元数据 | 新增 `MetadataPanel`；右侧栏显示当前日记元数据，可编辑日期、情绪、天气、精力、地点、标签和收藏状态；修改后会写回 Markdown frontmatter 并标记为未保存；前端测试覆盖 | `项目文档/08-功能增加与优化清单.md`、`项目文档/12-软件功能第四期-检索标签与整理.md`、`项目文档/个人AI日记产品规划.md` |
| P1 | 检索与标签 | 元数据编辑面板 | 右侧栏在 AI Cockpit 上方展示轻量元数据面板；已有 frontmatter 时显示“已连接 frontmatter”，无 frontmatter 时编辑后自动生成稳定 frontmatter | `项目文档/12-软件功能第四期-检索标签与整理.md` |
| P1 | 检索与标签 | frontmatter 元数据同步到 SQLite | `file_metadata` 新增 `diary_date`、`mood`、`tags`、`weather`、`location`、`energy`、`favorite` 字段；`save_markdown_file` 和 `save_markdown_file_as_copy` 保存后解析 frontmatter 并同步到 SQLite；Rust 测试 `save_markdown_file_syncs_frontmatter_metadata_to_sqlite` 覆盖 | `项目文档/12-软件功能第四期-检索标签与整理.md`、`项目文档/个人AI日记产品规划.md` |
| P1 | 检索与标签 | 当前文档标签管理基础能力 | `MetadataPanel` 支持从当前 Markdown frontmatter 标签列表新增、改名、删除、设置颜色和点击查看当前标签；修改会写回当前文档 frontmatter；前端测试 `manages current diary tags from the metadata panel` 覆盖 | `项目文档/08-功能增加与优化清单.md`、`项目文档/12-软件功能第四期-检索标签与整理.md`、`项目文档/初创/03-第三期-数据管理与搜索.md` |
| P1 | 检索与标签 | 全库标签列表与按标签查看日记/笔记 | 新增 `list_tags` / `search_files` Tauri 命令和 mock gateway；`SearchPanel` 展示全库标签及数量，点击标签会按该标签列出匹配日记/笔记；Rust 测试 `list_tags_and_search_files_read_repository_frontmatter` 和前端测试覆盖 | `项目文档/08-功能增加与优化清单.md`、`项目文档/12-软件功能第四期-检索标签与整理.md`、`项目文档/初创/03-第三期-数据管理与搜索.md` |
| P1 | 搜索 | 关键词搜索、日期范围筛选、标签筛选、文件类型筛选、搜索历史、搜索结果高亮 | `SearchPanel` 提供关键词、日期起止、标签和文件类型筛选；搜索结果展示标题、路径、日期/标签、正文片段和 `<mark>` 高亮；历史保留最近 5 个查询；前端和 Rust 测试覆盖 | `项目文档/08-功能增加与优化清单.md`、`项目文档/12-软件功能第四期-检索标签与整理.md`、`项目文档/初创/03-第三期-数据管理与搜索.md` |
| P1 | 检索与标签 | 全局标签表与跨文档标签维护 | SQLite 新增 `tags` / `file_tags` 表；`SearchPanel` 支持全局标签颜色、跨文档改名和删除；`rename_tag` / `delete_tag` 会改写全部匹配 Markdown frontmatter 并重新同步元数据；前端测试和 Rust 测试 `global_tag_maintenance_rewrites_markdown_frontmatter` 覆盖 | `项目文档/08-功能增加与优化清单.md`、`项目文档/12-软件功能第四期-检索标签与整理.md`、`项目文档/初创/03-第三期-数据管理与搜索.md` |
| P1 | 搜索 | FTS5 全文索引与索引增量更新 | SQLite 新增 `file_fts` FTS5 虚拟表；`search_files` 使用 `file_fts MATCH` 执行关键词搜索并叠加标签/日期/类型筛选；`save_markdown_file`、标签改名/删除和索引刷新会同步索引；Rust 测试 `save_markdown_file_updates_fts_index_incrementally` 覆盖 | `项目文档/12-软件功能第四期-检索标签与整理.md`、`项目文档/初创/03-第三期-数据管理与搜索.md` |
| P1 | 搜索 | 命令面板快速搜索 | 新增 `CommandPalette`；标题栏和 `Ctrl/Cmd+P` 可快速搜索并打开 Markdown 文件，`Ctrl/Cmd+Shift+P` 可搜索并执行保存、创建、模式切换、设置等命令；前端测试覆盖快速打开和命令执行 | `项目文档/初创/03-第三期-数据管理与搜索.md` |
| P1 | 收件箱与整理 | 收件箱 / 快速记录、整理到今天日记、整理视图 | 新增 `InboxPanel`；快速记录写入真实仓库 `Inbox/inbox.md`；Tauri 命令 `list_inbox_entries`、`append_inbox_entry`、`move_inbox_entries_to_today`、`clear_inbox_entries`、`list_organize_view` 已注册；整理视图展示未整理内容、无标签日记、最近修改和收藏日记；前端测试 `captures inbox items and moves them into today's journal` 和 Rust 测试 `inbox_entries_can_be_added_moved_to_today_and_cleared` 覆盖 | `项目文档/08-功能增加与优化清单.md`（收件箱 / 快速记录）、`项目文档/12-软件功能第四期-检索标签与整理.md`（2.5 收件箱 / 快速记录、2.6 整理视图、3.4 收件箱流程）、`项目文档/个人AI日记产品规划.md`（5.4 收件箱 / 碎片记录） |
| P1 | Markdown 渲染 | KaTeX、Mermaid、Emoji、脚注、20+ 语言高亮 | `MarkdownService` 接入 `katex`、`markdown-it-footnote`、`highlight.js/lib/common` 和 Emoji 短码映射；Mermaid fenced code 会生成 `.mermaid` 容器并由 `EditorWorkspace` 按需动态加载 Mermaid 渲染；前端测试覆盖公式、图表容器、Emoji、脚注和 20+ 语言高亮 | `项目文档/初创/02-第二期-编辑器三模式.md`（交付物：markdown-it 渲染引擎、highlight.js 代码高亮；验收项：KaTeX、Mermaid、Emoji、脚注）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 2：完整插件链、highlight.js） |
| P2 | 性能与体积 | 前端常驻入口 chunk 拆分 | `vite.config.ts` 增加 Rollup `manualChunks`，将 React、Ant Design 组件、rc-*、markdown-it、highlight.js、Tauri API 等分包；`npm run build` 后应用主入口约 218.21 kB，Ant Design 相关常驻大块已拆散 | `README.md`（体积优化说明）、`项目文档/08-功能增加与优化清单.md`（性能体验）、`项目文档/初创/06-第六期-打磨与发布.md`（性能优化）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：性能优化） |
| P2 | 性能与体积 | KaTeX 按需渲染加载 | `MarkdownService.render` 同步输出安全公式占位，`renderAsync` / `renderStandaloneHtmlAsync` / `renderCollectionHtmlAsync` 仅在检测到公式时动态加载 KaTeX 和 `katex.min.css` 并替换为真实 KaTeX HTML；`EditorWorkspace` 预览和 HTML/PDF/合集导出已接入异步渲染；构建入口不再静态 import 或 preload KaTeX；前端测试覆盖同步占位、异步 KaTeX 和异步导出 | `README.md`（体积优化说明）、`项目文档/08-功能增加与优化清单.md`（性能体验）、`项目文档/初创/06-第六期-打磨与发布.md`（性能优化）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：性能优化） |
| P2 | 性能与体积 | KaTeX 动态 runtime 拆分 | `MarkdownService.renderAsync` 改为动态加载 `katex/dist/katex.min.js`；`vite.config.ts` 单独拆出 `vendor-katex-runtime`，保留 KaTeX CSS / 字体按需产物；`npm run build` 后原 518.09 kB KaTeX 动态公式 chunk 拆为约 259.24 kB / 259.26 kB 两个按需 chunk，不再触发 500 kB 警告；Markdown 和编辑器相关测试、全量前端测试均通过 | `README.md`（体积优化说明）、`项目文档/08-功能增加与优化清单.md`（性能体验）、`项目文档/初创/06-第六期-打磨与发布.md`（性能优化）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：性能优化） |
| P2 | 性能与体积 | 编辑器工作区和 CodeMirror 按模式动态导入 | `App` 改为通过 `React.lazy` / `Suspense` 动态加载 `EditorWorkspace`，加载期间显示稳定占位；`EditorWorkspace` 不再静态引入 CodeMirror 依赖，源码 / 双视图模式中的 `CodeMirrorPane` 会在挂载后动态加载 `@codemirror/state`、`@codemirror/view`、`@codemirror/lang-markdown`、`@codemirror/theme-one-dark` 和 `codemirror`，保留同步 textarea fallback；`npm run build` 后 `EditorWorkspace` chunk 从 807.54 kB 降到约 204.24 kB，CodeMirror 相关依赖拆为约 208.22 kB / 197.32 kB 等按需 chunk；前端测试覆盖异步 CodeMirror 渲染、App 与编辑器主要交互路径 | `README.md`（体积优化说明）、`项目文档/08-功能增加与优化清单.md`（性能体验）、`项目文档/初创/06-第六期-打磨与发布.md`（性能优化）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：性能优化） |
| P2 | 性能与体积 | Mermaid 图表运行时外置与按需加载 | 新增 `scripts/prepare-mermaid-vendor.mjs`，在 `dev` / `build` 前把 Mermaid min runtime 和 chunks 复制到 `public/vendor/mermaid`；`EditorWorkspace` 改为通过 `import(/* @vite-ignore */ "/vendor/mermaid/mermaid.esm.min.mjs")` 在检测到 `.mermaid` 容器后再加载图表运行时；生成目录已加入 `.gitignore`，`make prepare-mermaid` 可手动准备资源，`make clean` 会清理；`npm run build` 不再出现 500 kB JS chunk 警告，构建产物包含 `dist/vendor/mermaid` 下 82 个 `.mjs` 文件、约 3.7M；前端全量测试通过 | `README.md`（体积优化说明）、`项目文档/08-功能增加与优化清单.md`（性能体验）、`项目文档/初创/06-第六期-打磨与发布.md`（性能优化）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：性能优化） |
| P2 | 性能与体积 | 文件树按展开状态懒渲染 | `RepositoryTree` 新增目录展开状态，默认只展开当前文件祖先目录；未展开目录不再递归渲染子节点，点击目录再渲染 children；搜索仍使用扁平节点索引展示未展开目录中的匹配 Markdown；前端测试覆盖折叠目录不预渲染、展开后显示和搜索完整性 | `README.md`（体积优化说明）、`项目文档/08-功能增加与优化清单.md`（性能体验）、`项目文档/初创/06-第六期-打磨与发布.md`（性能优化）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：性能优化） |
| P2 | 性能与体积 | 搜索结果缓存与索引失效 | `RepositoryService` 为 `searchFiles` 增加仓库级缓存，按 query / tag / date / fileType 和索引版本生成稳定 key；重复的全库检索、命令面板快速打开和 AI 上下文候选搜索会复用缓存结果；`listTags` 增加标签列表缓存；保存、另存副本、收件箱整理、标签改名/删除、创建/删除/恢复/重命名文件等索引变更路径会失效对应仓库缓存；前端测试覆盖重复搜索只调用一次 gateway、保存后搜索缓存失效、标签列表缓存和全局标签维护刷新 | `项目文档/08-功能增加与优化清单.md`（性能体验、搜索体验）、`项目文档/12-软件功能第四期-检索标签与整理.md`（全文搜索、标签筛选）、`项目文档/初创/06-第六期-打磨与发布.md`（性能优化）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：性能优化） |
| P1 / P2 | 错误处理 | 权限不足、目录不存在、磁盘空间不足、附件保存失败的中文处理建议 | 新增 `AppError` 和 `commandResultToError`，`RepositoryService.unwrap` 会根据 `errorCode` 和原始错误文本归类为 permission / missing_path / disk_space / attachment 等场景，并把可执行建议附加到 UI 已展示的错误消息中；前端测试覆盖权限、目录缺失、磁盘空间不足和附件失败 | `项目文档/08-功能增加与优化清单.md`（错误处理体验）、`项目文档/13-软件功能第五期-安全备份与长期使用.md`（2.6 错误处理优化、4.3 错误提示）、`项目文档/00-开发约束与协作规范.md`（错误处理规则） |
| P1 / P2 | 错误处理 | 最近错误诊断和复制诊断信息 | `appErrorService` 新增 `createErrorDiagnostic` / `serializeErrorDiagnostic`；`App` 会在搜索、保存、备份、恢复、导出、附件和全屏写作失败时记录最近错误诊断，包含操作名、错误场景、建议、仓库和文件上下文；顶部通知和命令面板提供“复制诊断”入口；前端测试覆盖诊断生成和序列化 | `项目文档/08-功能增加与优化清单.md`（错误处理体验）、`项目文档/13-软件功能第五期-安全备份与长期使用.md`（2.6 错误处理优化、4.3 错误提示）、`项目文档/00-开发约束与协作规范.md`（错误处理规则） |
| P1 / P2 | 错误处理 | 持久化错误日志、查看错误日志、批量清理日志 | `appErrorService` 新增 `loadErrorDiagnostics` / `appendErrorDiagnostic` / `clearErrorDiagnostics`，会在 localStorage 保留最近 20 条错误诊断；命令面板提供“查看错误日志”，弹窗展示日志列表、单条复制诊断和“清空日志”；前端测试覆盖日志持久化、截断、清理和弹窗入口 | `项目文档/08-功能增加与优化清单.md`（错误处理体验）、`项目文档/13-软件功能第五期-安全备份与长期使用.md`（2.6 错误处理优化、4.3 错误提示）、`项目文档/00-开发约束与协作规范.md`（错误处理规则） |
| P1 / P2 | 错误处理 | 错误日志按场景筛选 / 搜索、诊断导出文件 | `appErrorService` 新增 `filterErrorDiagnostics` / `serializeErrorDiagnosticLog`；错误日志弹窗可按错误场景筛选、按操作/错误/建议/文件关键词搜索，并把当前筛选结果导出为 `moknow-error-log-{日期}.txt`；前端测试覆盖筛选、搜索、导出 Blob 内容和批量序列化 | `项目文档/08-功能增加与优化清单.md`（错误处理体验）、`项目文档/13-软件功能第五期-安全备份与长期使用.md`（2.6 错误处理优化、4.3 错误提示）、`项目文档/00-开发约束与协作规范.md`（错误处理规则） |
| P1 / P2 | 错误处理 | 操作级错误恢复动作 | `appErrorService.getErrorRecoveryActions` 会根据错误操作和场景生成恢复动作；错误日志每条记录可直接执行重试保存、重新备份、重新恢复、重试 HTML 导出、重试 PDF 导出、重新打开日记合集导出和打开仓库位置；前端测试覆盖恢复动作规则与错误日志按钮展示 | `项目文档/08-功能增加与优化清单.md`（错误处理体验）、`项目文档/13-软件功能第五期-安全备份与长期使用.md`（2.6 错误处理优化、4.3 错误提示）、`项目文档/00-开发约束与协作规范.md`（错误处理规则） |
| P1 / P2 | 备份恢复 | 一键备份仓库、zip 备份、备份前检查未保存文件、备份后显示文件位置、恢复最近备份 | 新增 Tauri 命令 `backup_repository` / `restore_latest_backup` 和对应 `RepositoryService` 方法；真实仓库会生成 `.moknow/backups/{仓库名}-backup-{时间}.zip` 并跳过历史备份目录；恢复时读取最近 zip、校验路径和 CRC、覆盖仓库内容并保留备份目录；标题栏和命令面板提供备份与恢复入口；有未保存内容时前端会先提示保存，恢复前会二次确认；前端测试覆盖成功备份、未保存拦截和恢复入口，Rust 测试覆盖 zip 内容和真实恢复 | `项目文档/08-功能增加与优化清单.md`（导出与备份）、`项目文档/13-软件功能第五期-安全备份与长期使用.md`（2.4 备份与恢复、3.4 备份方案、4.2 备份体验）、`项目文档/个人AI日记产品规划.md`（6.3 导出与备份） |
| P1 / P2 | 备份恢复 | 备份进度条、自选备份位置、备份隐私过滤策略 | 备份入口改为设置弹窗，默认填入 `.moknow/backups` 路径并允许自选 zip 输出位置；备份过程展示检查、过滤/打包、完成进度；Tauri 命令 `backup_repository` 支持 `outputPath` 和 `excludePrivateData`，会跳过 `.env`、`*.key`、`*.pem`、token、secret 等疑似凭据文件并返回跳过数量；前端测试覆盖弹窗和自选路径，Rust 测试覆盖自选路径写入与隐私过滤 | `项目文档/08-功能增加与优化清单.md`（导出与备份）、`项目文档/13-软件功能第五期-安全备份与长期使用.md`（2.4 备份与恢复、3.4 备份方案、4.2 备份体验）、`项目文档/个人AI日记产品规划.md`（6.3 导出与备份） |
| P1 / P2 | 备份恢复 | 自选恢复备份文件、恢复前差异预览和恢复冲突策略 | 新增 Tauri 命令 `preview_restore_backup` 和 `restore_backup`；恢复弹窗可选择指定 zip 或默认读取最近备份，展示新增/修改/不变/覆盖时删除数量和示例路径；恢复策略支持“覆盖恢复”和“合并恢复：保留当前已有文件”；前端测试覆盖预览、选文件和合并恢复，Rust 测试覆盖自选备份预览与合并恢复不覆盖当前文件 | `项目文档/08-功能增加与优化清单.md`（导出与备份）、`项目文档/13-软件功能第五期-安全备份与长期使用.md`（2.4 备份与恢复、3.4 备份方案、4.2 备份体验）、`项目文档/个人AI日记产品规划.md`（6.3 导出与备份） |
| P1 / P2 | 导出 | 单篇 HTML 导出、标题层级/代码块/表格/图片相对路径保留 | `MarkdownService.renderStandaloneHtml` 生成完整 HTML 文档；Tauri 命令 `export_markdown_html` 将 HTML 写入当前 Markdown 同目录同名 `.html` 文件，重名时自动追加序号；标题栏和命令面板提供导出入口；前端测试覆盖标题栏导出，Rust 测试 `export_markdown_html_writes_html_next_to_source` 覆盖真实文件写入 | `项目文档/08-功能增加与优化清单.md`（导出与备份）、`项目文档/13-软件功能第五期-安全备份与长期使用.md`（2.3 导出、3.3 导出方案）、`项目文档/个人AI日记产品规划.md`（6.3 导出与备份） |
| P1 / P2 | 导出 | 单篇 PDF 导出 | 新增 Tauri 命令 `export_markdown_pdf`；前端复用 `MarkdownService.renderStandaloneHtml` 生成导出源，后端内置轻量 PDF 生成器会把当前文档文本快照写为同目录 `.pdf` 文件并自动避让重名；标题栏和命令面板提供“导出 PDF”；错误日志支持“重试导出 PDF”；前端测试覆盖标题栏导出，Rust 测试 `export_markdown_pdf_writes_pdf_next_to_source` 覆盖真实 PDF 文件写入 | `项目文档/13-软件功能第五期-安全备份与长期使用.md`（2.3 导出、3.3 导出方案、验收项：导出 PDF）、`项目文档/初创/05-第五期-插件系统与主题.md`（官方插件：导出 PDF）、`项目文档/个人AI日记产品规划.md`（6.3 导出与备份） |
| P1 / P2 | AI 日记助手 | 今日总结、温柔追问、周回顾、月回顾、AI 输出插入正文、上下文范围控制 | `AiCockpitService.runDiaryAction` 基于当前文件、最近 7 天、本月日记或全仓库上下文生成规则型日记助手内容；`AiCockpit` 提供上下文范围切换和日记助手动作按钮，助手消息可一键插入当前正文；前端测试覆盖上下文加载、周回顾生成和插入正文 | `项目文档/个人AI日记产品规划.md`（4.4 AI 日记助手、5.2 时间回顾、阶段二：AI 日记助手）、`项目文档/08-功能增加与优化清单.md`（P3：AI 日记助手、今日总结、温柔追问、周/月回顾） |
| P1 / P2 | AI 能力 | 真实 AI Provider 配置、模型设置、流式对话和 API Key 安全使用流程 | 新增 `AI_PROVIDER_SETTINGS_STORAGE_KEY` / `AI_PROVIDER_CREDENTIAL_KEY`；`AiCockpitService` 可保存 OpenAI-compatible endpoint、模型和流式开关，API Key 通过 `RepositoryService.saveSecureCredential` 写入系统凭据；普通提问会读取凭据并请求 chat completions，支持 SSE token 增量回调；AI 面板提供 Provider 设置、Key 保存 / 删除和配置状态提示；前端测试覆盖 Key 不落 localStorage、真实 provider 请求和流式 token 解析 | `项目文档/初创/04-第四期-AI-Copilot.md`（交付物：多模型 LLM 接入、流式响应、API Key 安全存储）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 4：LLM 接入）、`项目文档/个人AI日记产品规划.md`（AI Key 安全存储、AI 上下文控制） |
| P1 / P2 | AI 能力 | AI Provider 端到端脚本本地兼容服务自测 | 新增 `scripts/ai-provider-mock-e2e.mjs` 和 `ai:e2e:mock` / `make ai-e2e-mock`，会启动本地 OpenAI-compatible mock Provider，调用 `scripts/ai-provider-e2e.mjs` 分别验证非流式 JSON 响应和 SSE 流式响应，检查 Authorization Bearer 鉴权、请求体和成功短语，并生成 `release/ai-e2e-mock/summary.json` / `.md`；可用 `--write-doc` 写回 `项目文档/AI外部模型联调记录.md` 的本地自测区 | `项目文档/初创/04-第四期-AI-Copilot.md`（多模型 LLM 接入、流式响应验收）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 4：LLM 接入）、`项目文档/00-开发约束与协作规范.md`（质量门禁） |
| P1 / P2 | AI 能力 | AI Provider 真实外部联调预检脚本 | 新增 `scripts/ai-provider-preflight.mjs` 和 `ai:e2e:preflight` / `make ai-e2e-preflight`，可检查真实外部 OpenAI-compatible Provider 联调所需 endpoint、model、API Key、安全协议和超时配置，生成 `release/ai-e2e-preflight.json` / `.md`，并可写回 `项目文档/AI外部模型联调记录.md`；当前环境缺少 endpoint、model 和 API Key，普通预检会记录缺项，`--strict` 会失败阻断发布 | `项目文档/初创/04-第四期-AI-Copilot.md`（多模型 LLM 接入、流式响应验收、API Key 安全存储）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 4：LLM 接入）、`项目文档/00-开发约束与协作规范.md`（质量门禁） |
| P1 / P2 | AI 能力 | AI 真实 Provider 自动探测与一键联调 | 新增 `scripts/ai-provider-auto-e2e.mjs` 和 `ai:e2e:auto` / `make ai-e2e-auto`，会依次探测 `AI_E2E_*` / `OPENAI_*` 环境变量、本机 Ollama `/api/tags` 和 LM Studio `/v1/models`；发现真实 Provider 后自动调用 `ai-provider-e2e.mjs` 生成 `release/ai-e2e.json` 和不含密钥的联调记录；未发现时生成 `release/ai-e2e-auto.json` / `.md` 并写回 `项目文档/AI外部模型联调记录.md`，作为真实联调缺口证据；`release:final-audit -- --refresh` 会优先执行该自动探测 | `项目文档/初创/04-第四期-AI-Copilot.md`（多模型 LLM 接入、流式响应验收、API Key 安全存储）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 4：LLM 接入）、`项目文档/00-开发约束与协作规范.md`（质量门禁） |
| P1 / P2 | AI 能力 | Provider 预设、上下文过长截断、请求失败重试 | `AI_PROVIDER_PRESETS` 提供 OpenAI、DeepSeek、OpenRouter 和 Ollama 本地预设；AI 设置面板可一键套用 endpoint、默认模型和是否需要 API Key；`createProviderPrompt` 会对单篇和整体 prompt 做长度截断并把截断说明写入请求；Provider 请求会对 429、5xx 和网络失败最多重试 2 次，成功时标注重试次数，失败后回退本地规则；前端测试覆盖预设套用、Keyless 本地 provider、上下文截断和瞬时失败重试 | `项目文档/初创/04-第四期-AI-Copilot.md`（流式响应、上下文控制、请求失败）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 4：LLM 接入）、`项目文档/个人AI日记产品规划.md`（AI 上下文控制、隐私优先） |
| P1 / P2 | AI 检索 | 本地 RAG 检索、问我的日记、来源引用跳转、本地轻量向量索引、语义检索和索引未完成提示 | `AiContextEntry` / `AiMessage.sources` 新增结构化来源字段；`AiVectorIndexService` 可为当前范围 Markdown 构建本地词频向量索引，用余弦相似度和中英文查询扩展召回相关笔记；`App.loadAiContext` 会在最近 7 天、本月和全仓库模式下优先用向量检索读取 Top 8 文件作为 AI entries，索引为空或未就绪时把提示写入 `retrievalStatus`；AI 来源引用展示文件名、路径和片段，带 `fileId` 的来源可点击并调用 `selectFile` 打开原文；前端测试覆盖结构化来源渲染、“全仓库提问 -> 检索 README -> 点击来源打开文件”、向量召回和索引未完成提示 | `项目文档/初创/04-第四期-AI-Copilot.md`（4 RAG 检索管道、向量索引）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 4：RAG 检索、AI 向量索引）、`项目文档/个人AI日记产品规划.md`（5.3 问我的日记、AI 回答引用来源） |
| P1 / P2 | AI 检索 | 外部 Embedding、持久化向量库、索引构建进度 UI | `AiVectorIndexService` 新增 Embedding 设置持久化、OpenAI-compatible / Ollama 预设、外部 Embedding 批量调用、API Key 凭据读取、文档集签名匹配的本地持久化向量缓存和 `onProgress` 构建进度回调；`AiCockpit` 可配置 Embedding Provider、预设、endpoint、模型、API Key 和持久化开关，并展示索引阶段、百分比和失败回退提示；`App.loadAiContext` 改为异步构建 / 搜索向量索引，外部 Embedding 失败时回退本地索引；前端测试覆盖持久化索引复用、外部 Embedding 构建和搜索、设置保存不泄露 Key、进度 UI 展示 | `项目文档/初创/04-第四期-AI-Copilot.md`（4 RAG 检索管道、向量索引）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 4：AI 向量索引）、`项目文档/个人AI日记产品规划.md`（5.3 问我的日记） |
| P1 / P2 | 安全 | 系统 Keychain / Credential Manager 凭据存储底座 | 新增 `CredentialService`，Tauri 命令 `save_secure_credential` / `read_secure_credential` / `delete_secure_credential` / `get_secure_credential_status` 通过 `keyring` 接入系统凭据管理器；前端 `CommandGateway` 和 `RepositoryService` 暴露统一接口，浏览器预览使用 mock secure store；前端测试覆盖保存、读取、状态查询和删除，Rust 测试覆盖凭据键命名空间和校验 | `项目文档/08-功能增加与优化清单.md`（隐私与安全）、`项目文档/13-软件功能第五期-安全备份与长期使用.md`（2.1 应用锁、3.1 应用锁）、`项目文档/个人AI日记产品规划.md`（AI Key 安全存储、隐私优先） |
| P1 / P2 | 导出 | 日记月度合集、日期范围 HTML 合集导出、自选输出位置 | `MarkdownService.renderCollectionHtml` 生成带目录的合集 HTML；新增 Tauri 命令 `export_html_to_path` 写入用户选择路径；标题栏提供“导出日记合集”，命令面板提供“导出本月日记合集”和“导出日期范围日记”；前端测试覆盖日期范围导出，Rust 测试覆盖自选路径写入 | `项目文档/08-功能增加与优化清单.md`（导出与备份）、`项目文档/13-软件功能第五期-安全备份与长期使用.md`（2.3 导出、3.3 导出方案）、`项目文档/个人AI日记产品规划.md`（6.3 导出与备份） |
| P1 / P2 | 导出 | 导出进度条、导出预览和格式选项 | 日记合集导出弹窗新增导出预览、网页 HTML / 打印友好 HTML 格式选择、目录开关、每篇日记分页选项；导出流程会按校验、搜索、读取、渲染、写入阶段更新进度；前端测试覆盖弹窗选项，MarkdownService 测试覆盖选项对 HTML 输出的影响 | `项目文档/08-功能增加与优化清单.md`（导出与备份）、`项目文档/13-软件功能第五期-安全备份与长期使用.md`（2.3 导出、3.3 导出方案）、`项目文档/个人AI日记产品规划.md`（6.3 导出与备份） |
| P1 / P2 | 安全 | 应用锁、启动解锁、最小化/离开窗口自动锁定、空闲自动锁定、锁定后隐藏正文 | 新增 `appLockService`、`AppLockSettingsModal` 和 `AppLockOverlay`；`App` 启动时读取锁配置并进入锁屏，窗口隐藏/失焦和空闲计时会触发锁定；锁定时正文工作区不渲染；标题栏和命令面板提供应用锁入口；前端测试覆盖启动解锁和窗口隐藏自动锁定 | `项目文档/08-功能增加与优化清单.md`（隐私与安全）、`项目文档/13-软件功能第五期-安全备份与长期使用.md`（2.1 应用锁、3.1 应用锁）、`项目文档/个人AI日记产品规划.md`（隐私优先、阶段四：隐私与长期使用） |
| P1 / P2 | 安全 | 应用锁密码迁移到系统凭据 | `appLockService` 新增 `passwordStorage` / `credentialKey` 元数据和 `APP_LOCK_CREDENTIAL_KEY`；新设置或修改应用锁密码时只把带 salt 的密码哈希写入系统 Keychain / Credential Manager，localStorage 仅保留启用状态、salt 和凭据引用；旧版 localStorage hash 仍可兼容启动解锁，关闭应用锁会删除系统凭据；前端测试覆盖系统凭据写入、旧 hash 兼容、禁用清理和 App 设置流程不再保存 `passwordHash` | `项目文档/08-功能增加与优化清单.md`（隐私与安全）、`项目文档/13-软件功能第五期-安全备份与长期使用.md`（2.1 应用锁、3.1 应用锁）、`项目文档/个人AI日记产品规划.md`（隐私优先、AI Key 安全存储） |
| P1 / P2 | 安全 | 生物识别解锁、锁屏密码重置策略 | `appLockService` 新增 WebAuthn 平台认证器注册 / 验证、恢复码生成 / 校验和 `resetAppLockPasswordWithRecovery`；应用锁设置弹窗可启用生物识别解锁并重新生成恢复码，锁屏页可使用生物识别解锁，也可用恢复码重置新密码后立即解锁；恢复码仅一次性展示，`saveAppLockSettings` 会剥离 `pendingRecoveryCode`，避免明文落入 localStorage；前端测试覆盖平台认证器注册验证、恢复码重设密码和锁屏重置流程 | `项目文档/08-功能增加与优化清单.md`（隐私与安全）、`项目文档/13-软件功能第五期-安全备份与长期使用.md`（2.1 应用锁、3.1 应用锁）、`项目文档/个人AI日记产品规划.md`（隐私优先、阶段四：隐私与长期使用） |
| P2 | 双链笔记 | `[[标题]]` 解析、已有笔记自动补全、点击跳转、反向链接列表 | 新增 `wikiLinkService` 负责双链标题解析、候选收集和触发词补全；`MarkdownService` 将 `[[标题]]` / `[[标题|别名]]` 渲染为可点击内部链接；`EditorWorkspace` 在输入 `[[` 时展示已有笔记候选，预览点击双链可打开目标文件，并在大纲侧栏展示反向链接；`App` 扫描仓库 Markdown 内容计算当前文件反链；前端测试覆盖渲染、补全、点击跳转和反链导航 | `项目文档/08-功能增加与优化清单.md`（双链笔记）、`项目文档/个人AI日记产品规划.md`（6.1 双链与反向链接） |
| P2 | 主题与设置 | 通用设置面板、主题持久化、跟随系统、自定义主题、自定义 CSS、设置导入导出 | 新增 `appSettingsService` 和 `AppSettingsModal`；标题栏齿轮打开总设置；外观设置保存到 localStorage；`ThemeService.resolvePreference` 支持 day / night / system / custom；主题按钮会持久化切换结果；设置面板提供日记设置和应用锁入口，支持编辑自定义 CSS、导入 CSS 文件、导出设置 JSON 和导入设置 JSON；保存或导入后 `App` 会注入 `#moknow-custom-css`；前端测试覆盖主题持久化、自定义主题、自定义 CSS 应用和设置导入导出 | `项目文档/初创/05-第五期-插件系统与主题.md`（设置系统、主题系统、自定义 CSS、设置导入导出）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 5：主题系统）、`项目文档/08-功能增加与优化清单.md`（主题与视觉细节优化） |
| P2 | 主题与设置 | 主题插件扩展、仓库级 / 文件级设置 | `PluginService` 支持 manifest `contributes.themes` 的字符串和对象写法，可携带明暗模式、主色、紧凑模式、圆角和字号配置，并按 `theme` 权限收集运行时插件主题；`ThemeService.resolvePreference` 可解析 `plugin:{pluginId}:{themeId}` 主题偏好并生成 Ant Design token / CSS variables；`appSettingsService` 新增 `repositorySettings` / `fileSettings` 作用域覆盖和 `resolveScopedUserSettings` 合成逻辑；`AppSettingsModal` 提供插件主题选项、当前仓库主题 / CSS、当前文件主题 / CSS；`App` 按当前仓库和文件应用有效主题与 CSS；前端测试覆盖插件主题、文件级 CSS 和作用域设置合成 | `项目文档/初创/05-第五期-插件系统与主题.md`（设置系统、主题系统、插件主题、仓库级设置）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 5：主题系统）、`项目文档/08-功能增加与优化清单.md`（主题与视觉细节优化） |
| P2 | 插件系统 | 插件 manifest、插件加载器、插件注册表、PluginContext API | 新增 `PluginManifest`、`PluginPermission`、`PluginRegistryItem` 和 `PluginContext` 类型；`PluginService` 支持 manifest 校验、注册表 localStorage 持久化、启用/停用/移除、权限守卫和启动异常隔离；插件管理器可粘贴 manifest JSON 安装并展示权限、贡献点和状态；前端测试覆盖服务规则与 UI 安装启用流程 | `项目文档/初创/05-第五期-插件系统与主题.md`（插件 API、插件加载器、插件注册表、插件管理 UI）、`项目文档/初创/MarkNote-技术设计方案.md`（插件系统设计、Phase 5）、`项目文档/00-开发约束与协作规范.md`（插件目录与 PluginContext 约束） |
| P2 | 插件系统 | manifest 命令、侧边栏、主题、Markdown 渲染和 AI 工具贡献注册到主应用运行时 | `PluginService.collectRuntimeContributions` 会按启用状态和权限收集 manifest 中的命令、侧边栏、Markdown、AI 和主题贡献；`App` 已把启用插件的命令贡献接入命令面板，并把侧边栏贡献展示到右侧插件面板占位区；插件主题贡献已接入主题设置和主题解析；`MarkdownService` 可按启用的 Markdown 扩展贡献渲染 callout / mark；`AiCockpit` 会展示插件 AI 工具按钮，宿主运行时会读取当前上下文执行 summary / prompt / tag 等工具；前端测试覆盖权限守卫、命令触发、侧边栏注册、插件主题应用、Markdown 插件渲染和 AI 工具执行 | `项目文档/初创/05-第五期-插件系统与主题.md`（插件 API、插件管理 UI、命令 / 面板贡献、Markdown 渲染 / AI 扩展）、`项目文档/初创/MarkNote-技术设计方案.md`（插件系统设计、Phase 5）、`项目文档/00-开发约束与协作规范.md`（PluginContext 约束与插件目录） |
| P2 | 插件系统 | 官方插件目录和一键安装入口 | `OFFICIAL_PLUGIN_CATALOG` 提供日历视图、字数统计、导出 PDF、Git 同步 4 个内置官方插件 manifest；`PluginService.getOfficialPluginCatalog` / `installOfficialPlugin` 支持读取目录并安装到本机注册表；`PluginManagerModal` 展示官方插件卡片、贡献点和“安装官方插件”按钮；`App` 接入官方插件安装流程；前端测试覆盖官方插件目录、未知官方插件拒绝和插件管理器安装官方字数统计 | `项目文档/初创/05-第五期-插件系统与主题.md`（交付目标：官方插件；官方插件 × 4：日历视图、字数统计、导出 PDF、Git 同步；插件管理 UI）、`项目文档/初创/MarkNote-技术设计方案.md`（官方插件：日历视图、字数统计、导出 PDF、Git 同步；Phase 5）、`项目文档/00-开发约束与协作规范.md`（官方插件目录结构） |
| P2 | 插件系统 | 第三方插件入口 Worker 沙箱 | `PluginManifest.entry` 会通过 `PluginService.activatePluginInSandbox` 放入独立 Worker 脚本执行；沙箱注入只读 `moknow` API，插件只能通过 `registerCommand`、`registerSidebarPanel`、`registerMarkdownRenderer`、`registerAiTool`、`registerTheme` 注册贡献点；宿主会再次按 manifest 权限校验贡献点，无权限、异常或超时会把插件标记为 failed；无 `network` 权限时沙箱会禁用 `fetch`、`XMLHttpRequest`、`WebSocket`、`EventSource` 和 `importScripts`，并隔离 `document`、`window`、`localStorage`、`sessionStorage`；`App.togglePluginEnabled` 已接入异步沙箱启用流程；前端测试覆盖沙箱注册贡献、越权失败和沙箱脚本硬化 | `项目文档/初创/05-第五期-插件系统与主题.md`（插件 API、插件加载器、插件管理 UI、第三方 WASM 后期方案）、`项目文档/初创/MarkNote-技术设计方案.md`（第三方插件 `.marknote-plugin`、WASM 沙箱、权限审批机制）、`项目文档/00-开发约束与协作规范.md`（插件沙箱安全约束） |
| P2 | 发布能力 | CHANGELOG、tag 触发的三平台 GitHub Release 工作流和手动 dry-run 工作流 | 新增 `CHANGELOG.md`；新增 `.github/workflows/release.yml`，在 `v*` tag 上使用 npm、Rust stable 和 `tauri-apps/tauri-action` 分别构建 macOS / Windows / Linux Tauri bundle，并创建草稿 GitHub Release；新增 `.github/workflows/release-dry-run.yml`，支持手动触发和 PR 路径触发三平台 bundle dry-run，不创建 Release；dry-run workflow 会运行测试、前端构建、Tauri bundle、`release:artifacts`、`release:install-smoke` 和 `release:signing`，并上传 `moknow-dry-run-*` artifacts、安装冒烟报告和签名验证报告；正式 Release workflow 会在签名模式下要求 `release:artifacts -- --require-updater-signatures`、`release:signing -- --require-signed --require-notarized` 和 release metadata；YAML 解析已验证 | `项目文档/初创/06-第六期-打磨与发布.md`（CI/CD、GitHub Release、CHANGELOG）、`项目文档/00-开发约束与协作规范.md`（发布 Checklist）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：发布） |
| P2 | 发布能力 | 远端 Release Dry Run 自动触发、下载和验证脚本 | 新增 `scripts/run-release-dry-run.mjs` 和 `release:dry-run` / `make release-dry-run`，默认先执行 `release:workflow-validate -- --strict` 和 `release:ci-preflight -- --strict` 阻断 workflow 结构错误、未推送 workflow / 远端 404 状态；预检通过后可通过 GitHub CLI 触发 `.github/workflows/release-dry-run.yml`、等待 run 完成、执行 `release:remote-status -- --mode dry-run --strict` 复核远端 run / artifacts，再下载 artifacts 到 `release/downloaded-artifacts`，调用 `release:downloads` 做完整性验证，并继续对下载目录执行 `release:install-smoke`、`release:signing`、`release:aggregate-evidence` 和 `release:final-audit`；支持 `--print-only` 验证命令编排，支持 `--skip-workflow-validate` / `--skip-preflight` / `--skip-remote-status` / `--skip-install-smoke` / `--skip-signing` / `--skip-aggregate-evidence` / `--skip-final-audit` 做排查 | `项目文档/初创/06-第六期-打磨与发布.md`（CI/CD 与多平台发布、发布 Checklist）、`项目文档/00-开发约束与协作规范.md`（发布流程、质量门禁）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：发布） |
| P2 | 发布能力 | 远端 Release Dry Run 预检记录 | 新增 `scripts/release-ci-preflight.mjs` 和 `release:ci-preflight` / `make release-ci-preflight`，可检查 GitHub CLI、登录权限、origin、当前分支、发布相关本地改动、workflow 本地内容、workflow 是否纳入 git 索引、远端 workflow 是否存在和 dry-run 历史；会生成 `release/ci-preflight.json` / `.md`，并可写入 `项目文档/发布CI预检记录.md`；当前预检证明本地 workflow 内容齐全，但未被 git 跟踪且远端 workflow 404，因此远端 dry-run 尚不能触发 | `项目文档/初创/06-第六期-打磨与发布.md`（CI/CD 与多平台发布、发布 Checklist）、`项目文档/00-开发约束与协作规范.md`（发布流程、质量门禁）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：发布） |
| P2 | 发布能力 | Release workflow 结构化验证 | 新增 `scripts/validate-release-workflows.mjs` 和 `release:workflow-validate` / `make release-workflow-validate`，通过 YAML 解析检查 Release / Release Dry Run workflow 的触发器、build / verify job、三平台矩阵、Node / Rust / Tauri 构建步骤、AI 联调证据、artifact 上传下载、三平台下载验证、evidence 聚合、最终审计以及正式 Release 的 updater config、签名公证、metadata 和分发严格门禁；生成 `release/workflow-validation.*` 与 `项目文档/发布Workflow结构验证记录.md`；Release / Release Dry Run 的 Ubuntu verify job 已执行该验证并上传报告，正式 Release 还会把报告上传到 Draft Release；该验证已接入 `release:closure` 与 `release:final-audit -- --refresh` | `项目文档/初创/06-第六期-打磨与发布.md`（CI/CD、三平台发布、GitHub Release、发布 Checklist）、`项目文档/00-开发约束与协作规范.md`（质量门禁、发布流程）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：发布） |
| P2 | 发布能力 | 本地发布检查脚本与发布流程文档 | 新增 `scripts/release-check.mjs`，校验 `package.json`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml` 版本一致，检查 `CHANGELOG.md`、GitHub Release workflow、Tauri bundle 和图标配置，并列出签名、公证、自动更新所需外部项；`package.json` 新增 `release:check` / `release:verify` / `release:local`，Makefile 新增 `release-check` / `release-verify` / `release-local`；新增 `项目文档/发布流程.md`；`npm run release:verify` 已通过静态检查、148 个前端测试和前端构建 | `项目文档/初创/06-第六期-打磨与发布.md`（发布 Checklist、CI/CD、GitHub Release、Homebrew 发布）、`项目文档/00-开发约束与协作规范.md`（发布流程、质量门禁）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：发布） |
| P2 | 发布能力 | AI / 发布产物验证脚本和 updater 插件接入 | 新增 `scripts/ai-provider-e2e.mjs`，可通过 `AI_E2E_ENDPOINT`、`AI_E2E_MODEL`、`AI_E2E_API_KEY` 对真实 OpenAI-compatible Provider 发起端到端请求并生成不含密钥的 `项目文档/AI外部模型联调记录.md`；新增 `scripts/release-artifact-check.mjs` 和 `release:artifacts` / `make release-artifacts`，可检查当前平台安装包并在严格模式要求 updater `.sig`；`npx tauri add updater` 已接入 `@tauri-apps/plugin-updater`、`tauri-plugin-updater` 和 `updater:default` capability；Release workflow 已上传三平台 artifacts 供下载验证，并在正式 tag 构建中要求 updater 签名产物 | `项目文档/初创/04-第四期-AI-Copilot.md`（多模型 LLM 接入验收）、`项目文档/初创/06-第六期-打磨与发布.md`（发布 Checklist、CI/CD、签名、公证、自动更新、GitHub Release、Homebrew 发布）、`项目文档/00-开发约束与协作规范.md`（发布流程、质量门禁）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 4 / Phase 6） |
| P2 | 发布能力 | 下载后的发布 artifacts 完整性验证 | 新增 `scripts/verify-downloaded-artifacts.mjs` 和 `release:downloads` / `make release-downloads`，可扫描下载后的 GitHub Actions artifacts 目录，识别 macOS / Windows / Linux 安装包、`downloads.json`、`latest.json`、Homebrew cask 和 updater `.sig`，生成 `release/download-verification.json` / `.md`，并可通过 `--write-doc` 写回 `项目文档/发布产物验证记录.md` 的自动验证区；`release:check` 已检查该脚本和 npm 命令存在 | `项目文档/初创/06-第六期-打磨与发布.md`（发布 Checklist、CI/CD 与多平台发布）、`项目文档/00-开发约束与协作规范.md`（发布流程、质量门禁） |
| P2 | 发布能力 | 安装包入口和打开读写冒烟验证脚本 | 新增 `scripts/release-install-smoke.mjs` 和 `release:install-smoke` / `make release-install-smoke`，可检查 macOS `.app` 的 `Info.plist`、bundle identifier、版本号、可执行文件、DMG 镜像，以及 Windows / Linux 安装器入口；会生成 `release/install-smoke.json` / `.md`，并可写回 `项目文档/发布产物验证记录.md` 的自动安装冒烟区；本机 macOS bundle 已通过自动入口检查，人工打开和仓库读写清单已标注为待执行 | `项目文档/初创/06-第六期-打磨与发布.md`（发布 Checklist、安装验证）、`项目文档/00-开发约束与协作规范.md`（发布流程、质量门禁） |
| P2 | 发布能力 | 人工安装打开读写验收记录脚本 | 新增 `scripts/record-install-smoke-evidence.mjs` 和 `release:install-manual` / `make release-install-manual`，可记录实机安装、打开、仓库创建/打开、Markdown 保存、重启恢复和检查更新结果，生成 `release/manual-install-smoke.*` 与 `项目文档/发布安装人工验收记录.md`；`--result passed` 必须配合 `--confirm-all` 或逐项确认 flag，避免误记通过；`release:install-smoke` 会自动读取该持久化人工记录并写入“实机打开与仓库读写”证据，最终审计刷新不会覆盖已记录的人工结论 | `项目文档/初创/06-第六期-打磨与发布.md`（发布 Checklist、安装验证）、`项目文档/00-开发约束与协作规范.md`（质量门禁） |
| P2 | 发布能力 | 签名、公证、Gatekeeper / Authenticode 验证脚本 | 新增 `scripts/release-signing-verify.mjs` 和 `release:signing` / `make release-signing`，可在 macOS 检查 app / DMG 的 `codesign`、Gatekeeper `spctl` 和 `stapler validate`，在 Windows 检查 `Get-AuthenticodeSignature`，并为 Linux 产物记录 SHA-256 / GPG / repo 签名策略；会生成 `release/signing-verification.json` / `.md`，并可写回 `项目文档/发布签名验证记录.md`；当前本机 macOS 报告明确显示 app 为 ad-hoc/linker-signed、DMG 未签名，严格签名/公证模式会失败 | `项目文档/初创/06-第六期-打磨与发布.md`（代码签名与公证、发布 Checklist、签名验证）、`项目文档/00-开发约束与协作规范.md`（发布 Checklist、质量门禁） |
| P2 | 发布能力 | 三平台 artifacts 聚合验证 job | 新增 `scripts/aggregate-release-evidence.mjs` 和 `release:aggregate-evidence` / `make release-aggregate-evidence`，可扫描下载后的 GitHub Actions artifacts，合并每个平台 job 内置的 `install-smoke.json` 和 `signing-verification.json` 为顶层 `release/install-smoke.json`、`release/signing-verification.json` 和 `release/aggregate-evidence.*`；Release Dry Run workflow 和正式 Release workflow 均新增矩阵后的 `verify` job，下载全部平台 artifacts 后执行 `release:downloads`、`release:aggregate-evidence` 和 `release:final-audit`，并上传总验证报告；正式 Release 还会把总验证报告上传到 Draft Release | `项目文档/初创/06-第六期-打磨与发布.md`（CI/CD、三平台发布、发布 Checklist）、`项目文档/00-开发约束与协作规范.md`（发布流程、质量门禁）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：发布） |
| P2 | 发布能力 | 外部发布配置 readiness 报告 | 新增 `scripts/release-external-readiness.mjs` 和 `release:readiness` / `make release-readiness`，可生成 AI、Tauri updater、Apple 签名、公证、Windows code signing、下载 URL、Homebrew tap 的脱敏配置清单；`--strict` 会在缺项时失败，`--write-doc` 可写入 `项目文档/发布外部配置清单.md`；`release:check` 已检查该脚本和 npm 命令存在，并把 `MOKNOW_HOMEBREW_TAP` 纳入 strict external | `项目文档/初创/06-第六期-打磨与发布.md`（签名、公证、自动更新、GitHub Release、Homebrew 发布、官网分发）、`项目文档/00-开发约束与协作规范.md`（发布 Checklist、质量门禁） |
| P2 | 发布能力 | GitHub Actions secrets 名称审计 | 新增 `scripts/release-secrets-audit.mjs` 和 `release:secrets` / `make release-secrets`，通过 GitHub CLI 查询仓库 Actions secrets 名称和更新时间，审计 updater 签名私钥、公钥、macOS Developer ID 签名、公证、Windows code signing 和 Homebrew tap 所需 secret 是否存在；报告只记录名称是否存在，不读取或记录 secret 值；`--strict` 会在缺失时失败；当前远端仓库 secrets 列表为空，正式 Release workflow 仍缺外部凭据 | `项目文档/初创/06-第六期-打磨与发布.md`（签名、公证、自动更新、GitHub Release、Homebrew 发布、发布 Checklist）、`项目文档/00-开发约束与协作规范.md`（发布流程、质量门禁） |
| P2 | 发布能力 | GitHub Actions secrets 同步脚本 | 新增 `scripts/sync-release-secrets.mjs` 和 `release:secrets:sync` / `make release-secrets-sync`，可从当前 shell 环境变量读取 AI 真实联调、Tauri updater、Apple 签名、公证、Windows code signing 和 Homebrew tap 所需值，默认只生成 `release/secrets-sync.*` 与 `项目文档/发布Secrets同步记录.md` 预览；只有显式传入 `--apply` 才调用 `gh secret set --body-file -` 写入远端，`--verify` 会同步后重新执行 `release:secrets -- --write-doc`，报告只记录 secret 名称、是否配置、是否写入和命令摘要，不记录 secret 值；`--strict` 可在关键变量缺失时失败 | `项目文档/初创/06-第六期-打磨与发布.md`（签名、公证、自动更新、GitHub Release、Homebrew 发布、发布 Checklist）、`项目文档/00-开发约束与协作规范.md`（发布流程、质量门禁） |
| P2 | 发布能力 | 外部闭环交接清单和环境变量模板 | 新增 `scripts/generate-external-closure-handoff.mjs` 和 `release:handoff` / `make release-handoff`，可生成 `release/external-closure-handoff.*`、`release/external-closure.env.example`、`项目文档/发布外部闭环交接清单.md` 和 `项目文档/发布外部闭环环境变量模板.env`；清单按 3 个未完成外部闭环项列出需要补齐的证据、GitHub Actions secrets 设置命令、本机环境变量模板、发布提交入库 pathspec 计划、AI 真实联调命令、远端三平台 dry-run / artifacts 验收命令、签名公证 / updater / Homebrew / 官网分发验收命令和最终 `release:closure -- --strict --write-doc` / `release:final-audit -- --refresh --strict --write-doc` 总门禁；`release:check` 已纳入该脚本和 npm 入口检查 | `项目文档/初创/04-第四期-AI-Copilot.md`（AI 真实联调验收）、`项目文档/初创/06-第六期-打磨与发布.md`（发布 Checklist、签名、公证、自动更新、GitHub Release、Homebrew 发布、官网分发）、`项目文档/00-开发约束与协作规范.md`（质量门禁） |
| P2 | 发布能力 | 远端 workflow、run、artifacts 和 Release 状态快照 | 新增 `scripts/release-remote-status.mjs` 和 `release:remote-status` / `make release-remote-status`，可通过 GitHub CLI 只读检查远端 Release Dry Run workflow、正式 Release workflow、最近 5 次 runs、最近完成 run 的 artifacts 和指定版本 GitHub Release，生成 `release/remote-status.*` 与 `项目文档/发布远端状态记录.md`；严格模式要求 workflow 存在且最近成功 run 包含 macOS、Windows、Linux 和 verification artifacts；`release:closure` 与 `release:final-audit -- --refresh` 已纳入该远端证据 | `项目文档/初创/06-第六期-打磨与发布.md`（CI/CD、三平台发布、GitHub Release、发布 Checklist）、`项目文档/00-开发约束与协作规范.md`（发布流程、质量门禁）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：发布） |
| P2 | 发布能力 | 项目进度外部闭环总门禁 | 新增 `scripts/release-closure-gate.mjs` 和 `release:closure` / `make release-closure`，可一次性运行 `release:submission-plan`、`release:push-readiness`、`release:tracking -- --strict`、`ai:e2e:preflight -- --strict`、`ai:e2e:auto`、`release:ci-preflight -- --strict`、三平台 `release:downloads` / `release:install-smoke`、`release:secrets -- --strict`、`release:readiness -- --strict`、`release:check -- --strict-external`、严格签名公证、metadata、分发可达性和 `release:final-audit -- --refresh --strict --write-doc`；默认收集全部失败并生成 `release/closure-gate.*` 与 `项目文档/发布外部闭环总门禁记录.md`，加 `--strict` 可作为正式发布前总阻断门禁 | `项目文档/初创/04-第四期-AI-Copilot.md`（AI 真实联调验收）、`项目文档/初创/06-第六期-打磨与发布.md`（发布 Checklist、CI/CD、签名、公证、自动更新、Homebrew / 官网分发）、`项目文档/00-开发约束与协作规范.md`（质量门禁）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 4 / Phase 6） |
| P2 | 发布能力 | Release workflow 自动生成 AI 真实联调证据 | Release workflow 和 Release Dry Run workflow 的 build / verify job 均新增 `npm run ai:e2e:auto -- --write-doc` 步骤，并通过 `AI_E2E_ENDPOINT`、`AI_E2E_MODEL`、`AI_E2E_API_KEY`、`AI_E2E_PROVIDER_LABEL` GitHub secrets 传入真实 Provider 配置；secrets 未配置时会生成缺口记录，配置后会在远端 artifacts 中上传真实 `release/ai-e2e.json` 和 `项目文档/AI外部模型联调记录.md`，供最终进度审计读取 | `项目文档/初创/04-第四期-AI-Copilot.md`（AI 真实联调验收）、`项目文档/初创/06-第六期-打磨与发布.md`（CI/CD、发布 Checklist）、`项目文档/00-开发约束与协作规范.md`（质量门禁） |
| P2 | 发布能力 | 项目进度最终证据审计 | 新增 `scripts/final-progress-audit.mjs` 和 `release:final-audit` / `make release-final-audit`，可读取 `release/ai-e2e*.json`、`release/ci-preflight.json`、`release/download-verification.json`、`release/install-smoke.json`、`release/signing-verification.json`、`release/external-readiness.json`、`release/latest.json` 和 `release/downloads.json`，把项目进度中 3 个未完成外部闭环项按“完成判定 / 关键证据 / 未闭环原因 / 下一步 / 来源需求方案”汇总为自动表格，并可写回本文档；Release Dry Run workflow、正式 Release workflow 和 `release:dry-run` 自动化均已接入该报告，远端三平台实跑后会随 artifacts 上传；`--strict` 可在任何未完成项存在时失败 | `项目文档/初创/04-第四期-AI-Copilot.md`（AI 真实联调验收）、`项目文档/初创/06-第六期-打磨与发布.md`（发布 Checklist）、`项目文档/00-开发约束与协作规范.md`（质量门禁）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 4 / Phase 6） |
| P2 | 发布能力 | 发布文件入库门禁和提交计划 | 新增 `scripts/release-required-files.mjs` 作为闭环必需文件共享清单，覆盖 workflow、发布脚本、项目文档、`package.json` / lockfile、Tauri / Cargo updater 配置、desktop capability、Keychain 凭据服务和前端检查更新入口；新增 `scripts/release-tracking-gate.mjs` 和 `release:tracking` / `make release-tracking`，可检查闭环文件是否存在、是否被 `.gitignore` 忽略、是否已纳入 git；新增 `scripts/release-submit-prep.mjs` 和 `release:submit-prep` / `make release-submit-prep`，可一键刷新提交安全、提交计划、提交预演、推送准备和 CI 预检报告，并写入 `项目文档/发布提交准备总览.md`；新增 `scripts/release-submission-safety.mjs` 和 `release:submission-safety` / `make release-submission-safety`，可扫描即将入库的 pathspec / 必需文件是否包含密钥、证书、私钥、生成产物或额外路径，并写入 `项目文档/发布提交安全检查记录.md`；新增 `scripts/release-submission-plan.mjs` 和 `release:submission-plan` / `make release-submission-plan`，可生成 `release/submission-pathspec.txt` 与 `项目文档/发布提交入库计划.md`，并用临时 index / 临时 object 目录执行 `git add --dry-run` 证明 pathspec 可被 Git 接受；新增 `scripts/release-submission-preview.mjs` 和 `release:submission-preview` / `make release-submission-preview`，可用临时 index / object 目录预演提交 tree、diff 统计和变更文件，并写入 `项目文档/发布提交预演记录.md`；新增 `scripts/release-push-readiness.mjs` 和 `release:push-readiness` / `make release-push-readiness`，可生成 `项目文档/发布推送准备记录.md`，记录当前分支、upstream、GitHub CLI 登录、pathspec 临时入库和远端 workflow 状态；非严格 tracking 生成 `release/tracking-gate.*` 与 `项目文档/发布文件入库检查记录.md`，严格模式要求全部必需文件已被 git 跟踪，用于提交推送前避免远端 workflow 404 或缺脚本/依赖 | `项目文档/初创/06-第六期-打磨与发布.md`（CI/CD、发布 Checklist）、`项目文档/00-开发约束与协作规范.md`（质量门禁、发布流程） |
| P2 | 发布能力 | 自动更新入口、Tauri updater 配置生成与 Homebrew 分发元数据生成 | 新增 `scripts/prepare-tauri-updater-config.mjs` 和 `release:updater-config` / `make release-updater-config`，可从 `MOKNOW_UPDATER_PUBKEY`、`MOKNOW_UPDATER_ENDPOINT` 或 `MOKNOW_RELEASE_DOWNLOAD_BASE_URL` 生成被忽略的 `src-tauri/tauri.updater.conf.json`，并让 CI 通过 `--config` 构建 updater artifacts；新增 `scripts/generate-release-metadata.mjs` 和 `release:metadata` / `make release-metadata`，可从 Tauri bundle 生成 `release/downloads.json`、Tauri updater `release/latest.json` 和 `release/homebrew/moknow.rb`；Release workflow 会用 GitHub Release 下载 URL 生成元数据，并把 JSON / cask 上传到 artifacts 和 Draft Release；新增 `AppUpdateService` 和命令面板“检查应用更新”，Tauri 桌面环境会调用 `@tauri-apps/plugin-updater`，浏览器预览会提示当前环境不支持；本机 `npm run release:metadata` 已通过，当前因缺少 updater `.sig`，`latest.json` 暂无平台更新条目 | `项目文档/初创/06-第六期-打磨与发布.md`（自动更新、GitHub Release、Homebrew 发布、官网分发）、`项目文档/00-开发约束与协作规范.md`（发布 Checklist、质量门禁）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：发布） |
| P2 | 发布能力 | 分发端点与 Homebrew / updater 实发验证脚本 | 新增 `scripts/release-distribution-verify.mjs` 和 `release:distribution` / `make release-distribution`，可验证 `downloads.json`、`latest.json`、Homebrew cask、下载 URL、updater endpoint、官网 / 项目主页 URL 和 Homebrew tap 配置；`--require-reachable` 会对真实 URL 执行 HEAD / GET 可达性检查，`--strict` 可把缺失或占位 URL 升级为失败；Release workflow 在生成 metadata 后会运行严格分发验证，矩阵后的 `verify` job 也会下载 artifacts 后再次验证并上传分发验证报告；当前本机报告显示下载 URL、updater endpoint、Homebrew tap 和 cask 真实 URL 仍未配置 | `项目文档/初创/06-第六期-打磨与发布.md`（自动更新、GitHub Release、Homebrew 发布、官网分发、发布 Checklist）、`项目文档/00-开发约束与协作规范.md`（发布流程、质量门禁） |

## 四、未完成任务

> 已核对 `项目文档/初创` 目录中的需求方案。下表只列当前仍缺少真实外部证据或外部发布配置的未完成项，并标注对应需求来源。

### 4.1 未完成来源对照表

| 优先级 | 未完成功能 | 来源文档中的需求方案 | 目前缺少的完成证据 | 当前阻断 |
|---|---|---|---|---|
| P1 / P2 | 真实外部模型端到端联调记录 | `项目文档/初创/04-第四期-AI-Copilot.md`：多模型 LLM 接入、流式聊天界面、API Key 安全存储、多模型切换与流式响应验收；`项目文档/初创/MarkNote-技术设计方案.md`：Phase 4 LLM 接入、流式响应；`项目文档/个人AI日记产品规划.md`：API Key 安全存储、AI 上下文控制 | 真实 Provider 的 `npm run ai:e2e` / `npm run ai:e2e:auto` 成功记录，以及 `release/ai-e2e.json` / `项目文档/AI外部模型联调记录.md` 中的真实非 mock 结果 | 当前未配置 `AI_E2E_ENDPOINT`、`AI_E2E_MODEL`、`AI_E2E_API_KEY`，本机也未探测到可用 Ollama / LM Studio |
| P2 | 三平台完整构建验证、CI 实跑验证、发布产物下载验证 | `项目文档/初创/06-第六期-打磨与发布.md`：CI/CD 自动构建三平台安装包、v1.0 发布、发布 Checklist；`项目文档/初创/MarkNote-技术设计方案.md`：macOS / Windows / Linux 跨平台目标、Phase 6 三平台构建；`项目文档/00-开发约束与协作规范.md`：GitHub Release 附带安装包 | 远端 Release Dry Run / Release workflow 成功运行记录，macOS / Windows / Linux artifacts 下载校验记录，以及三平台安装打开读写验收记录 | workflow 文件尚未提交推送到远端，远端 workflow 查询仍为 404，Windows / Linux 产物和实机验收记录缺失 |
| P2 | macOS 签名、公证、Windows code signing、真实自动更新实发、Homebrew / 官网分发 | `项目文档/初创/06-第六期-打磨与发布.md`：自动更新、代码签名、公证、GitHub Release、Homebrew 发布、官网分发；`项目文档/初创/MarkNote-技术设计方案.md`：Phase 6 自动更新、官网；`项目文档/00-开发约束与协作规范.md`：安装包签名和公证、自动更新配置已发布 | 签名 / 公证严格验证通过记录，Tauri updater `.sig` 与 `latest.json` 平台条目，真实 HTTPS 下载 URL、Homebrew tap / cask、官网分发页和可达性验证记录 | Apple / Windows 签名凭据、Tauri updater key、GitHub Actions secrets、稳定下载 URL、Homebrew tap 和官网分发页尚未配置并实跑 |

### 4.2 未完成详情

| 优先级 | 功能模块 | 未完成内容 | 当前代码状态 | 来源需求方案 |
|---|---|---|---|---|
| P1 / P2 | AI 能力 | 真实外部模型端到端联调记录 | OpenAI-compatible Provider 配置、模型设置、流式调用、API Key 安全存储、Provider 预设、上下文截断和请求重试已完成；新增 `npm run ai:e2e` 可对真实 OpenAI-compatible Provider 发起端到端请求并生成 `项目文档/AI外部模型联调记录.md`；新增 `npm run ai:e2e:auto` 可自动探测环境变量、本机 Ollama 和 LM Studio，发现真实 Provider 后直接执行 E2E；新增 `npm run ai:e2e:mock` 已可验证联调脚本的鉴权、非流式和流式解析链路；新增 `npm run ai:e2e:preflight -- --write-doc` 已记录当前 endpoint、model 和 API Key 缺失，严格预检会失败；新增 `npm run release:handoff -- --write-doc` 已生成真实 Provider 环境变量模板和 AI 验收命令；新增 `release:secrets:sync` 可同步 `AI_E2E_*` 到 GitHub Actions secrets；新增 `release:closure` 可把 AI 严格预检、自动探测和最终审计纳入一键总门禁；Release / Release Dry Run workflow 已在 build / verify job 中接入 `ai:e2e:auto`，配置 secrets 后可自动产出远端真实 AI 联调证据；当前环境未配置 `AI_E2E_ENDPOINT` / `AI_E2E_MODEL` / `AI_E2E_API_KEY`，且未发现可用 Ollama / LM Studio，因此尚未产生真实外部 API Key 或本机真实模型联调记录 | `项目文档/初创/04-第四期-AI-Copilot.md`（1.1 核心交付物：多模型 LLM 接入、流式聊天界面、API Key 安全存储；九、验收标准：多模型切换、流式响应、API Key 安全）、`项目文档/初创/MarkNote-技术设计方案.md`（3.4 AI Copilot 架构；Phase 4：LLM 接入、流式响应）、`项目文档/个人AI日记产品规划.md`（API Key 安全存储、AI 上下文控制） |
| P2 | 发布能力 | 三平台完整构建验证、CI 实跑验证、发布产物下载验证 | macOS aarch64 release 构建和本机 `npm run release:artifacts` 已通过；GitHub Actions 三平台 Release workflow 已添加 artifact 上传、CI 产物检查、安装冒烟报告、签名验证报告和 updater 签名要求；新增 Release Dry Run workflow，可在正式 tag 前手动触发 macOS / Windows / Linux 三平台构建并上传 `moknow-dry-run-*` artifacts、`release/install-smoke.*` 和 `release/signing-verification.*`；新增 `release:workflow-validate` 可结构化验证 workflow YAML 的三平台矩阵、build / verify job 和 evidence 链路；新增 `release:dry-run` 默认执行严格 CI 预检，避免在 workflow 未推送时直接触发远端失败；预检通过后可通过 GitHub CLI 自动触发 dry-run、等待完成、下载 artifacts，并调用 `release:downloads`、`release:install-smoke` 和 `release:signing`；新增 `release:remote-status` 可只读检查远端 workflow、runs、artifacts 和 GitHub Release 状态，并把结果写入 `项目文档/发布远端状态记录.md`；新增 `release:submit-prep` 可一键刷新提交安全、提交计划、提交预演、推送准备和 CI 预检报告；新增 `release:submission-safety` 可扫描即将入库文件中的密钥、证书、私钥、生成产物和额外 pathspec 风险；新增 `release:submission-plan` 已生成 NUL 分隔 pathspec 与 `项目文档/发布提交入库计划.md`，给出提交 workflow / 发布脚本 / 项目文档的安全 `git add` 命令；新增 `release:submission-preview` 可用临时 index / object 目录预演提交 tree、diff 统计和变更文件；新增 `release:push-readiness` 已生成 `项目文档/发布推送准备记录.md`，确认当前分支、upstream、GitHub CLI 登录和 67 个必需文件 pathspec 临时入库均通过，同时记录远端 workflow 仍未存在；新增 `release:install-manual` 可持久记录实机安装打开读写结论，`release:install-smoke` 会读取该记录；新增 `release:handoff` 已生成远端三平台 CI、artifacts 下载、安装冒烟和证据聚合的执行清单；新增 `release:closure` 会把提交安全检查、提交计划、推送准备、入库严格检查、workflow 结构验证、远端 CI 预检、远端状态快照、三平台下载校验、安装冒烟和最终审计作为一键总门禁；新增 `release:ci-preflight` 已确认 GitHub CLI 登录和本地 workflow 内容，但 workflow 当前未纳入 git 索引且远端 404；因此 dry-run / tag workflow 尚未在远端实际跑通，Windows/Linux CI 产物下载、完整性检查与实机安装打开验证仍未执行；`项目文档/发布提交安全检查记录.md`、`项目文档/发布提交入库计划.md`、`项目文档/发布推送准备记录.md`、`项目文档/发布Workflow结构验证记录.md`、`项目文档/发布产物验证记录.md`、`项目文档/发布CI预检记录.md` 与 `项目文档/发布远端状态记录.md` 已记录当前远端阻断原因 | `项目文档/初创/06-第六期-打磨与发布.md`（1.1 核心交付物：CI/CD 自动构建三平台安装包、v1.0 发布；六、CI/CD 流水线；八、发布 Checklist）、`项目文档/初创/MarkNote-技术设计方案.md`（1.2 跨平台目标：macOS / Windows / Linux；Phase 6：CI/CD 构建三平台安装包）、`项目文档/00-开发约束与协作规范.md`（13.2 发布 Checklist：GitHub Release 附带安装包） |
| P2 | 发布能力 | macOS 签名、公证、Windows code signing、真实自动更新实发、Homebrew / 官网分发 | `CHANGELOG.md`、GitHub Release 工作流、本地发布检查脚本、发布流程文档、Tauri updater 插件依赖、`updater:default` capability、应用内“检查应用更新”入口、updater 临时配置生成脚本、artifact 校验脚本、下载完整性脚本、安装冒烟脚本、签名验证脚本、分发元数据生成脚本、分发端点验证脚本、GitHub secrets 审计脚本、GitHub secrets 同步脚本、外部 readiness 报告、外部闭环交接清单、外部闭环总门禁和严格外部凭据检查已新增；`release:secrets` 可审计远端 Actions secrets 名称是否存在；`release:secrets:sync` 可在本机外部变量配置好后先预览、再通过 `--apply --verify` 写入 GitHub Actions secrets；`release:closure` 会把 secrets 严格审计、readiness 严格检查、`release:check -- --strict-external`、签名公证、metadata、分发可达性和最终审计串成正式发布前总门禁；`release:handoff` 可生成 GitHub secrets 设置命令、本机环境变量模板、签名公证 / updater / Homebrew / 官网分发验收命令和最终 strict audit 总门禁；`release:readiness` 可生成脱敏外部配置清单并在 strict 模式缺项时失败；`release:updater-config` 可为 CI 生成含公钥和 endpoint 的 Tauri 配置，Release workflow 已通过 `--config` 使用它并要求 `.sig`；`release:metadata` 可生成下载清单、`latest.json` 和 Homebrew cask；`release:distribution -- --strict --require-reachable` 可验证真实下载 URL、updater endpoint、Homebrew cask 和官网 URL；`release:signing -- --write-doc` 已记录当前 macOS app / DMG 未满足正式签名公证，严格模式会失败；签名证书、公证凭据、`TAURI_SIGNING_PRIVATE_KEY`、`MOKNOW_UPDATER_PUBKEY`、`MOKNOW_HOMEBREW_TAP`、真实 Homebrew tap、官网分发页、HTTPS updater 端点和稳定下载 URL 仍需外部配置后实跑 | `项目文档/初创/06-第六期-打磨与发布.md`（1.1 核心交付物：自动更新、代码签名、v1.0 发布；五、自动更新；七、代码签名与公证；八、发布 Checklist / Homebrew 发布）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：自动更新、官网）、`项目文档/00-开发约束与协作规范.md`（13.2 发布 Checklist：自动更新配置已发布、GitHub Release 已创建） |

## 五、文档不一致点

- `README.md` 原先说真实本地仓库读写仍是 mock；当前代码已实现真实仓库读写底座，本次已修正。
- `README.md` 原先说当前测试为 7 个文件、9 个用例；当前是 16 个文件、148 个用例，本次已修正。
- `README.md` 原先说 Tauri 桌面构建因 Windows MSVC 未闭环；当前 macOS aarch64 已构建成功，但 Windows 仍需单独验证，本次已修正。
- `项目文档/个人AI日记产品规划.md` 中“多 AI Provider、AI 上下文控制”等已具备能力描述偏乐观；按当前代码可认定 OpenAI-compatible Provider 配置、模型设置、Provider 预设、流式调用、API Key 安全存储、上下文截断、请求重试、本地 RAG、轻量向量检索、外部 Embedding、持久化向量缓存、索引构建进度、语义召回、索引未完成提示和来源跳转流程已完成，但真实外部模型端到端验证仍未闭环；插件系统已完成 manifest / 注册表 / 管理 UI / 运行时贡献 / 官方插件目录 / 第三方 Worker 沙箱。
- `项目文档/06-第六期-打磨与发布.md` 是空文件；真实内容在 `项目文档/初创/06-第六期-打磨与发布.md`。

## 六、下一步建议

建议按以下顺序推进：

1. AI 能力：先执行 `npm run release:handoff -- --write-doc` 查看真实 Provider 环境变量模板；配置真实 Provider 环境变量，或启动本机 Ollama / LM Studio 后执行 `npm run ai:e2e:auto -- --write-doc`；需要把 AI 联调放到远端 CI 时，先用 `npm run release:secrets:sync -- --write-doc` 预览 `AI_E2E_*` secrets，同步后 Release / Release Dry Run workflow 会自动执行 `ai:e2e:auto`；需要显式外部验收时继续执行 `npm run ai:e2e:preflight -- --strict --write-doc`、`npm run ai:e2e` 和 `npm run ai:e2e -- --stream`，补齐 `项目文档/AI外部模型联调记录.md` 的真实记录。
2. 发布能力：先执行 `npm run release:handoff -- --write-doc` 查看 workflow、artifacts、安装冒烟和证据聚合验收顺序；提交前执行 `npm run release:submission-safety -- --write-doc`、`npm run release:submission-plan -- --write-doc`、`npm run release:push-readiness -- --write-doc` 和 `npm run release:tracking -- --write-doc`，再用 `git add --pathspec-from-file=release/submission-pathspec.txt --pathspec-file-nul` 纳入闭环文件，随后执行 `npm run release:tracking -- --strict --write-doc` 确认 workflow、脚本和项目文档都已入库；提交并推送 `.github/workflows/release-dry-run.yml`、`.github/workflows/release.yml` 和发布脚本后，执行 `npm run release:dry-run -- --write-doc`；命令会自动先跑严格 CI 预检，再触发 Release Dry Run workflow。随后推送测试 tag 触发三平台 Release CI，下载 GitHub Actions artifacts / Draft Release 产物，运行 `npm run release:downloads -- --write-doc`，实机安装打开读写通过后执行 `npm run release:install-manual -- --result passed --confirm-all --artifact-root release/downloaded-artifacts --platform all --write-doc`，再运行 `npm run release:install-smoke -- --artifact-root release/downloaded-artifacts --platform all --write-doc` 固化人工结论。
3. 发布能力：先执行 `npm run release:handoff -- --write-doc` 查看 GitHub secrets 设置命令和本机环境变量模板；配置 Apple / Windows 签名、公证、Tauri updater key、真实 Homebrew tap、官网分发页和稳定下载 URL 后执行 `npm run release:secrets:sync -- --write-doc` 预览，同步前确认报告无误，再执行 `npm run release:secrets:sync -- --apply --verify --write-doc`；随后执行 `npm run release:secrets -- --strict --write-doc`、`npm run release:readiness -- --strict --write-doc`、`npm run release:check -- --strict-external`、`npm run release:updater-config` 和 `npm run release:signing -- --require-signed --require-notarized --write-doc`，并用 `npm run release:metadata -- --require-base-url --require-updater-signatures` 验证更新元数据。

## 七、自动最终进度审计

<!-- AUTO_FINAL_PROGRESS_AUDIT_START -->
# 项目进度最终审计

| 项目 | 值 |
|---|---|
| 生成时间 | 2026-06-12T04:44:55.134Z |
| 刷新报告 | 否 |
| 严格模式 | 否 |
| 未完成项数量 | 3 |

| 优先级 | 功能模块 | 未完成内容 | 自动审计结论 | 关键证据 | 未闭环原因 | 下一步 | 来源需求方案 |
|---|---|---|---|---|---|---|---|
| P1 / P2 | AI 能力 | 真实外部模型端到端联调记录 | 未完成 | AI 预检：未通过（Endpoint 配置（`AI_E2E_ENDPOINT` / `OPENAI_BASE_URL` 缺失）、Endpoint URL 格式（无法解析为 URL）、Model 配置（`AI_E2E_MODEL` / `OPENAI_MODEL` 缺失）、API Key 配置（`AI_E2E_API_KEY` / `OPENAI_API_KEY` 缺失））<br>自动探测：未发现可用真实 Provider<br>真实 E2E JSON：缺失 | 当前没有真实外部 Provider 的成功请求证据；mock E2E 只能证明脚本解析链路。 | 配置 AI_E2E_ENDPOINT / AI_E2E_MODEL / AI_E2E_API_KEY，或启动本机 Ollama / LM Studio 后执行 npm run ai:e2e:auto -- --write-doc；需要显式校验时再执行 ai:e2e:preflight -- --strict、ai:e2e 和 ai:e2e -- --stream。 | `项目文档/初创/04-第四期-AI-Copilot.md`（1.1 核心交付物：多模型 LLM 接入、流式聊天界面、API Key 安全存储；九、验收标准：多模型切换、流式响应、API Key 安全）、`项目文档/初创/MarkNote-技术设计方案.md`（3.4 AI Copilot 架构；Phase 4：LLM 接入、流式响应）、`项目文档/个人AI日记产品规划.md`（API Key 安全存储、AI 上下文控制） |
| P2 | 发布能力 | 三平台完整构建验证、CI 实跑验证、发布产物下载验证 | 未完成 | 提交前准备：通过（tree 8ccf8d924f9f29eaad4742f96ac15f3af021f7c3，必需文件 73 个）<br>提交计划：通过（73 个必需文件，pathspec 已覆盖 73 个可入库文件，未入库 61 个），git add dry-run 通过，临时 index 严格入库模拟 通过<br>提交安全：通过（扫描 73 个文件）<br>提交校验：通过（73 个文件，72 个参与聚合，SHA-256 15034c6aa5cd1208ea62999ac75ee6c3947dd820e1de505137fd297c6f6ba192）<br>提交预演：通过（tree 8ccf8d924f9f29eaad4742f96ac15f3af021f7c3，73 个必需文件进入临时 index）<br>推送准备：通过（分支 main，upstream origin/main，pathspec 临时入库 通过，警告 3 项）<br>CI 预检：未通过（.github/workflows/release-dry-run.yml 已纳入 git 索引（git ls-files 未找到）、.github/workflows/release.yml 已纳入 git 索引（git ls-files 未找到）、远端 Release Dry Run workflow（{"message":"Not Found","documentation_url":"https://docs.github.com/rest/actions/workflows#get-a-workflow","status":"404"}））<br>Workflow 结构：通过<br>远端状态：未通过（远端 Release Dry Run workflow（{"message":"Not Found","documentation_url":"https://docs.github.com/rest/actions/workflows#get-a-workflow","status":"404"}）、远端 Release Dry Run artifacts（无可用 run）、远端 Release workflow（{"message":"Not Found","documentation_url":"https://docs.github.com/rest/actions/workflows#get-a-workflow","status":"404"}）、远端 Release artifacts（无可用 run））<br>下载产物：未通过（Windows 安装包、Linux 安装包）<br>安装冒烟：通过/待人工项不阻断脚本<br>实机打开：待人工 | 远端 workflow、Windows/Linux artifacts、三平台下载校验和实机打开读写验证尚未同时具备成功证据。 | 先执行 npm run release:submit-prep -- --write-doc，或分步执行 release:submission-safety、release:submission-plan、release:submission-manifest、release:submission-preview 和 release:push-readiness；使用 git add --pathspec-from-file=release/submission-pathspec.txt --pathspec-file-nul 纳入 workflow / 发布脚本 / 项目文档，再执行 npm run release:tracking -- --strict --write-doc；提交并推送后执行 npm run release:ci-preflight -- --strict --write-doc、npm run release:dry-run -- --write-doc 和 npm run release:remote-status -- --strict --write-doc；下载三平台 artifacts 后执行 release:downloads 与 release:install-smoke -- --platform all，并补人工打开结果。 | `项目文档/初创/06-第六期-打磨与发布.md`（1.1 核心交付物：CI/CD 自动构建三平台安装包、v1.0 发布；六、CI/CD 流水线；八、发布 Checklist）、`项目文档/初创/MarkNote-技术设计方案.md`（1.2 跨平台目标：macOS / Windows / Linux；Phase 6：CI/CD 构建三平台安装包）、`项目文档/00-开发约束与协作规范.md`（13.2 发布 Checklist：GitHub Release 附带安装包） |
| P2 | 发布能力 | macOS 签名、公证、Windows code signing、真实自动更新实发、Homebrew / 官网分发 | 未完成 | 外部配置：缺失（Tauri updater 签名、Tauri updater 公钥与端点、macOS Developer ID 签名、macOS 公证、Windows code signing、下载与官网分发 URL、Homebrew tap）<br>GitHub secrets：缺失（Tauri updater 签名私钥、Tauri updater 公钥、macOS Developer ID 签名、macOS 公证、Windows code signing、Homebrew tap）<br>签名/公证：未通过（macOS app 签名（src-tauri/target/release/bundle/macos/MoKnow.app）、macOS DMG 签名（src-tauri/target/release/bundle/dmg/MoKnow_0.1.0_aarch64.dmg））<br>分发验证：未通过（下载 URL（MoKnow_0.1.0_aarch64.dmg: 缺失）、latest.json updater 平台（0 个平台条目）、Homebrew cask URL（https://github.com/OWNER/REPO/releases/download/v0.1.0/MoKnow_0.1.0_aarch64.dmg）、官网 / 项目主页（https://github.com/OWNER/REPO）、Release 下载 Base URL（缺失）、Updater endpoint（缺失）、Homebrew tap（缺失））<br>latest.json 平台条目：0<br>downloads.json：已生成 | 正式证书、公证凭据、updater key、稳定下载 URL、Homebrew tap / 官网分发仍未全部实跑验证。 | 配置 GitHub Actions secrets、Apple / Windows 签名、公证、TAURI_SIGNING_PRIVATE_KEY、MOKNOW_UPDATER_PUBKEY、稳定 HTTPS 下载 URL、Homebrew tap 和官网分发页后，先执行 release:secrets:sync -- --write-doc 预览；确认无误后执行 release:secrets:sync -- --apply --verify --write-doc，再执行 release:secrets -- --strict、release:readiness -- --strict、release:signing -- --require-signed --require-notarized、release:metadata -- --require-base-url --require-updater-signatures 和 release:distribution -- --strict --require-reachable。 | `项目文档/初创/06-第六期-打磨与发布.md`（1.1 核心交付物：自动更新、代码签名、v1.0 发布；五、自动更新；七、代码签名与公证；八、发布 Checklist / Homebrew 发布）、`项目文档/初创/MarkNote-技术设计方案.md`（Phase 6：自动更新、官网）、`项目文档/00-开发约束与协作规范.md`（13.2 发布 Checklist：自动更新配置已发布、GitHub Release 已创建） |

> 自动审计只把可验证证据作为完成依据；缺少远端运行、真实外部服务、证书或人工实机记录时会继续判定为未完成。
<!-- AUTO_FINAL_PROGRESS_AUDIT_END -->
